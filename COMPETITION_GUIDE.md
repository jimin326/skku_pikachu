# 2026 성균관대 × 한양대 CSE 교류전 AI 부문 — 대회 가이드 정리본

- 출처: 운영 사이트 shaims (https://shaims.vercel.app) 가이드 탭 9페이지 + 홈·제출·대진 페이지, Q&A 시트(구글 스프레드시트) 전문
- 갱신: 2026-09-05 (사이트·시트를 팀 계정으로 로그인해 덤프한 시점 기준). 같은 날 엔진 설명(tick·반응 지연·전역 수명·rallyFrameCount·numpy·서브·파워히트)을 저장소 코드와 공식 main 기준으로 교정. 조 편성·제출 0건·4MB 등 사이트 전용 정보는 09-05 덤프 기준 미재검증.
- 사이트는 팀명+비밀번호 로그인 뒤에만 보임. 가이드는 당일 스킬 공개 시 갱신된다고 명시되어 있으니 대회 전날·당일에 다시 확인할 것.
- 우리 팀: 사자먹는은행 (조 A)

---

## 0. 한눈에 보기 (핵심만)

| 항목 | 값 |
|---|---|
| 게임 | 원작 피카츄 배구 기반 "리온이 배구" |
| 봇 형태 | 파일 1개, 최상위 `decide(snapshot) → {x, y, hit}` |
| 언어 | JavaScript(.js) 또는 Python(.py, Pyodide. 코드상 numpy 로드 없음, 8장) |
| 파일 크기 | 4MB 이하 |
| 프레임 / 틱 | 25 FPS(40ms), 봇 결정 주기 3프레임 = 120ms |
| 시간 예산 | 목표 120ms, hard timeout 360ms, 15틱 연속 초과 시 워커 재시작(전역 상태 초기화) |
| 반응 지연 | 정상 속도에서 응답은 보통 다음 처리 프레임(약 40ms)에 적용되고 다음 결정까지 약 3프레임 유지. 느린 응답·슬로모션에서는 더 길어짐 (7장) |
| 세트 | 10점 선착 |
| 서브권 | 첫 서브와 매 득점 후 서브 모두 독립 랜덤 |
| 특수 규칙 | 한 진영 연속 5회 접촉 시 실점 |
| 세트 시간 | 4분 경과 시 앞선 팀 승, 동점이면 5분까지 골든볼, 그래도 동점이면 무승부 |
| 예선 | 3조 × 4팀, 1경기 = 1세트, 팀당 6경기 |
| 본선 | 8강 토너먼트, 3세트 2선승 |
| 당일 변수 | 스킬 추가(히든 룰 없음), 스냅샷에 새 필드만 추가, 공개 후 첫 경기까지 1시간 |
| 당일 도구 | 웹 검색 불가, LLM 질문 팀당 3회·회당 300자, 외부 링크 불가, 코랩은 내장 AI 차단 + 현장 검사 조건부 허용 |
| 경기 방식 | 한 PC에서 양 팀 봇 파일을 봇 설정 패널에 넣고 Apply 후 시작 |

---

## 1. 대회 개요 (가이드 "소개")

- 원작 피카츄 배구(github.com/gorisanson/pikachu-volleyball)를 기반으로 참가자가 짠 봇끼리 대결.
- 사람이 키보드로 조작하는 대신, 매 틱 게임 상황을 스냅샷으로 받은 `decide(snapshot)` 함수가 조작을 대신함.
- 언어는 JS 또는 Python 중 하나. 결과물은 파일 한 개 `<팀명>_v<버전>.<js|py>`.
- 가이드 구조: 소개 → 환경 세팅·실행 → API(decide·스냅샷) → Python 특이사항 → 예제(Minimal → Positioning → Power Hit) → 대회 규칙 → 자주 걸리는 함정.
- 준비물: Node.js 18+(LTS), 에디터, Chrome 최신 버전(필수, 당일 중계도 동일 환경). Python 봇도 로컬 Python 설치 불필요.

가이드에 실린 최소 예시:

```js
function decide(s) {
  var dx = s.ball.expectedLandingPointX - s.self.x;
  var x = 0;
  if (Math.abs(dx) > 8) x = dx > 0 ? 1 : -1;
  return { x: x, y: 0, hit: 0 };
}
```

---

## 2. 대진 · 진행 방식

### 2.1 조 편성 (2026-09-05 대진 페이지 기준, 전 경기 미진행)

| 조 | 팀 |
|---|---|
| A | 깐쵸새우, **사자먹는은행(우리)**, 성대보다맛있는한양대주먹밥, 쨈미니 |
| B | 섭지코지, 정성가득, 품바와티몬, Avian Influenza |
| C | 군필복학생조승용여친구함, 럭키펀치, 웅진씽크빅, 천꾸 제이스 |

- 조 A에 편성된 경기 2개: 깐쵸새우 vs 사자먹는은행, 성대보다맛있는한양대주먹밥 vs 쨈미니 (점수 미기록).
- 순위표 컬럼: P W D L GF GA GD Pts (경기·승·무·패·득점·실점·득실차·승점). 무승부(D)가 있는 것은 5분 무승부 규칙 때문.

### 2.2 예선 — 조별리그

- 총 3조, 조당 4팀. 1경기 = 1세트. 한 팀당 6경기 진행 예정(사이트 명시는 "6경기". 조 내 3팀과 2회씩으로 추정).
- 조 1·2위 자동 진출. 각 조 3위 중 승점·득실차 비교 상위 2팀 추가 진출 → 8팀.

### 2.3 본선 — 토너먼트 8강

- 1경기 = 3세트, 2세트 선취 시 매치 승리.

### 2.4 세트 시간 제한

- 세트 시작 후 4분 경과 → 그 시점 스코어에서 앞선 팀 승.
- 4분에 동점 → 5분까지 골든볼(먼저 득점한 팀 승).
- 5분에도 결판 없으면 무승부.
- 대진표·시간표·상금 등 운영 사항은 별도 안내.

---

## 3. 매치 규칙 (게임 자체)

- 원작 룰 기본 준수 + 아래 대회 규칙.
- 1세트 = 10점 선착제.
- 서브권은 첫 서브와 매 득점 후 다음 서브 모두 **독립적으로 랜덤** 추첨(원작의 득점자 서브 규칙 아님). `pikavolley.js`의 `serveRule = SERVE_RULE.RANDOM`, 득점 처리와 `startOfNewGame` 양쪽에서 `decideNextServe`가 `rand() % 2`로 결정. 득점 순간 `meta.isPlayer2Serve`가 즉시 갱신됨.
- **연속 5회 접촉 실점**: 공이 네트를 넘지 않은 채 같은 편에서 5번째로 공에 닿는 순간 상대 득점. 자기 토스로 랠리를 무한히 끄는 것을 막는 규칙. (우리 저장소 엔진 `src/resources/js/rules/touchLimit.js`의 MAX_TOUCHES_PER_SIDE = 5 와 동일. "도달"이 조건이므로 5번째 접촉 즉시 실점.)
- 25 FPS(1프레임 40ms), 봇 결정 주기 3프레임(120ms). `config.tickFrameGroupSize`로 스냅샷에 실려 오며 하드코딩 금지 권고.

---

## 4. 제출

### 4.1 shaims 제출 페이지 (/submissions)

- 팀별 제출 기록 페이지. **최신 제출본이 대전에 사용됨.**
- 파일 선택 → 업로드. 허용 확장자 .js 또는 .py, 최대 4.00 MB.
- 파일명은 팀명+확장자로(예 `teamA.js`). 저장 시 자동으로 `_v버전`이 붙어 내역에 표시됨. 규약 형식을 직접 맞출 필요 없음.
- 여러 번 제출 가능, 매번 새 버전으로 기록, 관리자는 전체 이력 확인 가능.
- 2026-09-05 기준 우리 팀 제출 0건.
- 최종 제출 방법·마감은 "대회 규칙 문서 별도 공지 참고" (가이드 범위 밖).

### 4.2 형식 규칙

- 파일 하나. 여러 파일 분할·`import` 불가.
- 언어는 확장자로 결정.
- 파일명 규약 `<팀명>_v<버전>.<js|py>` (예 `Alpha_v1.js`). 로컬 봇 설정 UI 드롭다운은 이 규약을 지킨 파일만 인식 (`_v` 뒤 버전 필수). 버전은 문자열이라 `1`, `1.2`, `20260814` 등 자유.
- 팀명·확장자에 공백·특수문자(따옴표·경로 구분자 등) 금지.

---

## 5. 봇 코드 계약

### 5.1 실행 모델

```
파일 로드 (Worker 생성 시 한 번 = Apply 시점)
  ├─ var/전역 초기화, 헬퍼 함수 정의
  └─ decide 함수 정의
       └─ [tick 1] decide(snapshot₁) → {x, y, hit}
          [tick 2] decide(snapshot₂) → {x, y, hit}
          ...
```

- `decide` 밖 전역 변수는 틱 사이에 유지됨(직전 공 위치 기억, 상대 패턴 카운트 등에 사용).
- **전역 상태 수명은 "매치당"이 아니라 "Apply-to-Apply"**. Worker는 봇 설정 패널에서 Apply할 때 만들어지고, 그 side의 설정(모드·파일)이 바뀌는 Apply가 있을 때만 파괴됨(`testSetup.js`, 같은 설정이면 재사용). 따라서 세트가 끝나고 다음 매치를 시작해도, 상대 side만 바뀌어도 **내 Worker와 전역 변수는 그대로 이어짐**. 학습형 상태를 쓰면 새 매치 감지(점수 0-0 복귀 등) 시 직접 초기화할 것.
- 강제 재시작(15틱 연속 hard timeout) 시 파일 재로드 → 전역 상태 전부 리셋.
- LEFT/RIGHT에 같은 파일을 써도 각자 다른 Worker에서 로드되어 전역 변수 공유 안 됨.
- `decide` 밖의 `console.log`는 Worker 로드당 한 번만 찍힘(매치당이 아님).

### 5.2 시그니처

```js
function decide(snapshot) { return { x: -1|0|1, y: -1|0|1, hit: 0|1 }; }
```
```python
def decide(snapshot): return {'x': -1, 'y': 0, 'hit': 0}
```

- 매개변수 이름 자유. 클래스·export 불필요. 최상위에 `decide`만 있으면 됨.

### 5.3 반환값

| 필드 | 허용값 | 의미 |
|---|---|---|
| x | -1, 0, 1 | 왼쪽 / 가만히 / 오른쪽 이동 |
| y | -1, 0, 1 | 위(점프·상방 스매시) / 가만히 / 아래(하방 스매시) |
| hit | 0, 1 | `state === 1`이면 파워히트 발동, `state === 0`에서 x≠0·y≠-1이면 다이빙 |

원작 4버튼과 1:1 대응이 아니며 파생 규칙이 있음:

- **점프 필드 없음**: `self.state === 0`(지상)에서 `y = -1`이 점프. 점프 중(`state === 1`)에는 `y = -1`을 보내도 재점프하지 않으며, 파워히트 상태로 전환된 뒤 실제 충돌 프레임의 y가 스매시 각도를 결정함.
- **스매시 각도·속도는 `hit = 1`을 보낸 순간이 아니라 실제 공 충돌 프레임의 입력으로 결정**(`physics.js` 충돌 처리에서 `state === 2`일 때 그 프레임의 `xDirection`·`yDirection`을 읽음). `y`: `-1` 위로 아치(못박기), `0` 직선, `1` 아래로 꽂는 강스매시. 봇 입력은 다음 결정까지 3프레임 유지되므로 충돌 프레임까지 같은 x·y를 계속 내보내야 함.
- **파워히트 방향은 x가 아니라 네트 위치로 결정**: 공이 네트 왼쪽이면 오른쪽 코트로, 오른쪽이면 왼쪽 코트로. `x` 부호는 무관하고, 충돌 프레임에 nonzero면 파워히트 속도 2배, 0이면 기본 속도.
- **`state === 2`(파워히트 중)에도 수평 이동은 됨**(`state < 3`이면 x 입력이 적용). 조작이 완전히 무시되는 것은 아님.
- **다이빙** = `state === 0` + `x ≠ 0` + `hit = 1` + **`y ≠ -1`** 네 조건. 엔진은 점프 판정을 먼저 하므로 지상에서 `{x:1, y:-1, hit:1}`을 보내면 다이빙이 아니라 같은 프레임에 점프 후 곧바로 파워히트 상태(state 2)가 됨.
- **`hit = 1`을 여러 틱 유지하는 것은 안전하지 않음.** state 2는 약 10프레임 뒤 공중에서 state 1로 돌아오는데, 그때 hit가 아직 1이면 파워히트가 다시 발동함. 착지해서 state 0이 된 순간 `x ≠ 0`이면 의도치 않은 다이빙이 나감. 파워히트는 필요한 틱에만 `hit = 1`을 보낼 것.

### 5.4 잘못된 값

값 범위 밖, 필드 누락, `undefined`/`None` 반환, 예외 발생 → 해당 응답을 폐기하고 최신 입력을 무입력(`x=0,y=0,hit=0`)으로 바꿈. 따라서 **다음 유효 응답이 도착할 때까지 여러 엔진 프레임 동안** 무입력이 적용될 수 있음. 이전 값 유지도, 봇 종료도 아님.

---

## 6. 스냅샷 전체

### 6.1 예시

```json
{
  "tick": 12345,
  "side": "LEFT",
  "self": { "x": 120, "y": 244, "state": 0, "frameNumber": 3, "divingDirection": 0 },
  "opp":  { "x": 300, "y": 180, "state": 1, "frameNumber": 5, "divingDirection": 0 },
  "ball": { "x": 216, "y": 140, "xVelocity": -3, "yVelocity": 6,
            "expectedLandingPointX": 340, "isPowerHit": false },
  "meta": { "score": { "self": 7, "opp": 6 }, "isPlayer2Serve": false, "rallyFrameCount": 42 },
  "config": { "tickFrameGroupSize": 3 }
}
```

### 6.2 좌표계

- 원점 화면 좌상단. x는 오른쪽으로 증가, **y는 아래로 증가**. 바닥이 0이 아님. 캐릭터 지상 y = 244, 공 최고점은 y가 가장 작음, 낙하 중이면 `yVelocity > 0`.
- 단위는 원작 픽셀 그대로, 정수.
- x = 216이 네트. 코트 영역은 LEFT (0, 216), RIGHT (216, 432)이지만 **플레이어 중심 x의 실제 이동 범위는 LEFT [32, 184], RIGHT [248, 400]**(PLAYER_HALF_LENGTH 32만큼 벽·네트에서 안쪽으로 클램프).

### 6.3 상수 (직접 상수로 박아 쓰라고 안내)

| 이름 | 값 | 뜻 |
|---|---|---|
| GROUND_WIDTH | 432 | 코트 전체 폭 |
| GROUND_HALF_WIDTH | 216 | 네트 x |
| PLAYER_TOUCHING_GROUND_Y_COORD | 244 | 플레이어 지상 y |
| BALL_TOUCHING_GROUND_Y_COORD | 252 | 공 지면 접촉 y |
| BALL_RADIUS | 20 | |
| PLAYER_HALF_LENGTH | 32 | 플레이어 히트박스 반폭·반높이 |
| NET_PILLAR_HALF_WIDTH | 25 | 네트 기둥 반폭 (기둥 x = 216 ± 25) |
| NET_PILLAR_TOP_TOP_Y_COORD | 176 | 네트 기둥 상단 y |

### 6.4 필드별

- `tick`: 봇 입력의 `getInput()`이 실제 처리될 때마다 1씩 증가하는 정수. 스냅샷은 `tick % tickFrameGroupSize === 0`인 프레임에만 전송되므로 봇이 받는 tick은 3, 6, 9, … 꼴. **정상 속도에서는** 경과 게임 시간 ≈ `tick * 40ms`(× tickFrameGroupSize 아님). 득점 후 슬로모션에는 `getInput()` 호출 자체가 5FPS로 줄어드므로 벽시계 경과 시간 계산에는 그대로 쓸 수 없음. 봇이 코트에 설치된 구간(랠리·득점 후 연출·READY 등)에서만 세고, Worker가 재사용되면 이전 매치에서 이어서 증가하므로 "매치 시작 후" 기준으로 쓰지 말 것.
- `side`: "LEFT" 또는 "RIGHT". 같은 파일이 어느 쪽에도 배치될 수 있으므로 네트 방향·코트 범위를 하드코딩하지 말고 매번 side로 판단.
- `self` / `opp` (완전 대칭, 상대 정보 숨기지 않음):

| 필드 | 값 | 뜻 |
|---|---|---|
| x, y | 정수 | 캐릭터 중심 |
| state | 0~6 | 상태(아래 표) |
| frameNumber | 정수 | 현재 상태의 애니메이션 프레임 |
| divingDirection | -1, 0, 1 | 다이빙 방향 |

| state | 뜻 | 조작 가능성 |
|---|---|---|
| 0 | 정상(서 있음·걷는 중) | 이동·점프·다이빙 자유 |
| 1 | 점프 중 | 이동 유지, 파워히트 가능 |
| 2 | 파워히트 발동 중 | 수평 이동 가능. 실제 공 충돌 프레임의 x·y가 타구 속도·각도를 결정. hit 재입력은 현재 상태 전환에 영향 없음. 약 10 처리 프레임 뒤 공중이면 1로 복귀 |
| 3 | 다이빙 중 | 궤적 따라감, 조작 무시 |
| 4 | 누워 있음(다이빙 착지 후) | 몇 프레임 못 움직임 |
| 5 | 승리 포즈 | — |
| 6 | 패배 포즈 | — |

- `ball`:

| 필드 | 값 | 뜻 |
|---|---|---|
| x, y | 정수 | 공 중심 |
| xVelocity, yVelocity | 정수 | 프레임당 이동량, yVelocity > 0 = 낙하 중 |
| expectedLandingPointX | 정수 | 엔진이 네트 충돌·중력까지 반영해 계산한 예상 낙하 x |
| isPowerHit | boolean | 마지막 타격이 파워히트였는지 |

  - `expectedLandingPointX`는 "지금 이 순간"의 예상. 플레이어·네트 접촉 후 다음 틱에 확 바뀌므로 캐시하지 말고 매 틱 새로 읽을 것.
  - 내 속도 = 직전에 내가 낸 x 입력(수평 관성 없음). 상대 속도는 직전 스냅샷 `opp.x`와 비교해서 직접 계산. 엔진 미제공.

- `meta`:

| 필드 | 뜻 |
|---|---|
| score.self / score.opp | 내 관점 스코어 (LEFT면 왼쪽 점수가 self) |
| isPlayer2Serve | 현재 랠리(득점 후 연출 중이면 다음 랠리)를 오른쪽(Player 2)이 서브하는지. 득점 순간 다음 서브권으로 갱신 |
| rallyFrameCount | 정확히는 "점수 합계 변경을 마지막으로 감지한 뒤 처리된 프레임 수". 득점 다음 처리 프레임에 변경을 감지해 0이 되고, 이후 슬로모션의 나머지 처리 프레임과 페이드 5 + READY 30을 지나 **다음 랠리 첫 엔진 프레임의 내부 값은 41**. 다만 스냅샷은 3프레임마다 전송되므로 봇이 새 랠리에서 처음 관측하는 값은 41·42·43 중 하나일 수 있음. 매치 첫 랠리의 첫 스냅샷은 3. 서브 판정에 0이나 특정 단일 값을 가정하지 말 것 |

- `config.tickFrameGroupSize`: 기본 3. 대회 준비 중 조정될 수 있으니 스냅샷에서 읽을 것.

### 6.5 당일 확장

- 당일 스킬 대응을 위해 스냅샷 API가 확장될 수 있음. **기존 필드는 변경되지 않고 새 필드만 추가.** 새 필드를 참조하지 않는 기존 봇도 그대로 매치 진행 가능(스킬 활용·방어 못 하는 만큼 불리).

---

## 7. 시간 예산 · 격리 · 재시작

- 목표: 틱 주기 120ms 안에 리턴. 응답이 오기 전까지는 새 요청을 보내지 않고 직전 결과를 계속 적용하므로, 느릴수록 결정 간격이 벌어지고 반응이 늦어짐.
- Hard timeout 360ms(= 40ms × 3 × 3). 초과 응답은 폐기되고 최신 입력이 무입력으로 바뀌므로, 다음 유효 응답이 올 때까지 여러 엔진 프레임 동안 무입력일 수 있음. 여유를 3배로 잡은 이유는 Pyodide 첫 콜 워밍업·GC 지연 흡수.
- 15틱 연속 hard timeout → 봇 워커 강제 재시작(파일 재로드, 전역 상태 초기화).
- 초기화 대기 UI의 시간 상한은 60초. Python은 Pyodide 로딩 포함(평상시 5~10초). 60초가 지나면 로딩 모달을 닫고 진행할 뿐 Worker를 그 순간 강제 종료하는 엄격한 실행 제한은 아님. 이후 틱 timeout이 연속되면 별도의 15회 규칙으로 Worker가 재시작됨. numpy는 로드하지 않음(8장).
- 봇은 각자 Web Worker에서 격리. 메인 스레드·상대 봇 방해 불가. 무한루프를 돌려도 상대는 정상 진행, 내 봇만 무입력 → 재시작(사실상 실격 수준 패널티).
- 반응 지연: 엔진이 봇 응답을 동기 대기할 수 없어 `getInput()`은 각 처리 프레임에 "가장 최근에 도착한 결과"를 적용함(`botInput.js`). 정상 속도에서 보통 3ms 안에 응답하면 **다음 처리 프레임(약 40ms 뒤)에 적용**되고, 새 결과가 올 때까지 보통 약 3프레임 유지됨. 응답이 느리면 80ms, 120ms 이상으로 늘어나며, 득점 후 슬로모션에서는 처리 프레임 자체가 5FPS라 벽시계 기준 다음 적용이 약 200ms 뒤일 수도 있음. 고정 1틱(120ms) 지연이 아니며 양쪽에 같은 구조가 적용됨. 낙하지점 걷기에 `Math.abs(dx) > 8` 같은 데드밴드를 두면 이 지연에 의한 진동이 줄어듦.
- Q&A 실측: 대회 PC(RAM 16GB, CPU 점유 낮춘 상태)에서 봇은 보통 3ms 내 리턴. 과반복 코드가 없으면 사양 제한 없음.

---

## 8. 언어별 허용 범위

### JavaScript
- 가능: 표준 전역(`Math`, `Date`, `JSON`, `console.log`, `Array`, `Map`, `Set`, `RegExp` 등).
- 불가: `import`/`require`(파일 격리). DOM(`window`, `document`)은 Worker 안이라 애초에 없음.
- **대회 규정상 금지**(코드가 차단하지는 않음): `fetch`/XHR, Worker 생성 등 외부 통신·스레드 생성. 봇은 일반 Web Worker 안에서 `new Function`으로 실행될 뿐 별도의 API 제거 샌드박스는 없음(`botWorker.js`). 쓰면 부정행위.

### Python (Pyodide 314.0.3, CPython 3.x, 브라우저 WebAssembly)
- 가능: 표준 라이브러리 전체(`math`, `random`, `heapq`, `collections`, `itertools`, `functools`, `statistics`, `bisect`, `dataclasses` 등).
- **numpy는 사이트 가이드와 달리 현재 Runner에서 바로 쓸 수 없음.** `botWorkerPython.js`(로컬·공식 main 동일)에 `loadPackage('numpy')` 호출이 없고 wheel도 번들되지 않아 `import numpy`가 ModuleNotFoundError로 초기화 실패함. UI의 "numpy 로딩" 문구만 남아 있는 상태. 당일 새 레포에서 바뀔 수 있으니 쓰려면 그때 다시 확인.
- 불가: `scipy`, `pandas`, `scikit-learn`, `torch`, `tensorflow` 등 서드파티, 파일 I/O, 네트워크, `subprocess`/`os.system`.
- `import` 실패 = 초기화 에러 → 매치 불가.
- `snapshot`은 dict(`s['ball']['x']`). 반환도 반드시 dict(tuple·list·dataclass는 무입력).
- 첫 적용 시 Pyodide 로딩 수 초("환경 세팅 중..." 모달). 같은 설정이면 페이지가 살아있는 동안 재로딩 없음.
- `print`는 F12 Console. 매 틱 print 금지.
- 흔한 실수: SyntaxError(문법), `name 'decide' is not defined`(최상위 정의 누락), `No module named 'scipy'`, 봇이 가만히 있음(dict 아닌 반환·예외), 느림(매 틱 무거운 연산 → 파일 로드 시점으로 이동).

---

## 9. 로컬 환경 세팅 · 실행 (요약)

1. Node.js LTS, Git, Chrome 최신 설치.
2. `git clone https://github.com/SKKU-x-HYU-SW-Competition/leonyi-volleyball.git` → `cd leonyi-volleyball` → `npm install`(1회).
3. `src/code-here/`에 `<팀명>_v<버전>.js|py` 생성(`Example_v1.js`, `Example_v1.py` 기본 포함).
4. `npm start` → `http://localhost:8080` (포트 점유 시 8081 등으로 자동 이동, 터미널 주소 사용). 파일 수정 시 자동 재빌드.
5. "게임 시작" → 상단 "봇 설정" 패널 → LEFT/RIGHT 각각 Keyboard / Bot(드롭다운 파일 선택) / AI(내장) → "적용(재시작)".
6. 키보드: LEFT = D G R F Z(좌 우 점프 아래 파워히트), RIGHT = ← → ↑ ↓ Enter.
7. 디버깅: F12 Console. 게임 인스턴스는 `window.__pikaVolley`로 접근 가능.
8. 안전한 루틴: 수정·저장 → dev 서버 "compiled" 확인 → 봇 설정 다시 열지 말고 적용(재시작)만 클릭 → Console 예외 확인 → 이상하면 Minimal부터 로직을 조금씩 얹어 이등분 탐색.

---

## 10. 예제 봇 해설

### 10.1 Minimal — 뼈대
- 항상 `{x:0, y:0, hit:0}` 반환. 서서 공 맞고 지는 봇. 필요 조건은 최상위 `decide` 하나뿐임을 보여줌.

### 10.2 Positioning — 파워히트 없이 몸으로만 막기
핵심 아이디어 3가지:
1. **파워히트 없이도 공은 넘어감.** 몸에 닿으면 튕기며, 방향은 공이 몸 중심의 어느 쪽에 닿았는지로 결정(왼쪽에 닿으면 왼쪽으로). 공 바로 밑에 서면 위로 튀어 내 코트로 떨어지기 쉬우니 낙하지점에서 네트 반대쪽으로 12px 물러서서 "네트 쪽 면"으로 받는다 (`targetX = landing - towardNet * 12`).
2. **공이 상대 코트에 있을 땐 참기.** 낙하지점을 무작정 따라가면 네트 앞에 붙게 되어 후속 대응이 어려움 → 내 코트 중앙(standbyX)으로 후퇴 대기(내장 AI와 같은 방식).
3. **점프는 엄격하게.** 상하 32px 안에 공이 들어오면 이미 몸으로 받힌 것으로 처리되므로 어설픈 점프는 착지 순간 공이 지나감. 조건 4개 전부 만족할 때만: 공 x가 내 x ± 32 안, `|xVelocity| < 5`, `ball.y < 150`, `yVelocity > 0`, 그리고 `state === 0`.
- 걷기 데드밴드 6px. 결과: 내장 AI 상대로 방어는 되지만 득점 못 함, Minimal 상대로 완승.

### 10.3 Power Hit — 참가자 몫
- 예제는 준비되어 있었지만 가이드에서 제거됨. "공개된 게임 엔진과 로직을 분석하여 최강의 봇을 작성"하라는 팁.
- **"당일 추가 스킬에 의해 수비형은 통하지 않을 가능성이 높습니다. 매서운 공격을 시도해보세요."** (운영진 명시)

---

## 11. 자주 걸리는 함정 (가이드 "caveats")

- **Y축 아래로 증가**: 최고점 y가 가장 작음, 낙하 중 yVelocity > 0, 지상 y = 244.
- **코트 분리**: x = 216 네트, 각 봇은 자기 코트에서만 이동.
- **파워히트 방향은 네트 기준**, x는 속도 2배 여부만.
- **강스매시(y=1)는 자책 위험**: 네트에서 멀면 넘기기 전에 내 바닥에 박힘 → 네트 약 80px 안에서만 y=1, 멀면 y=-1 아치.
- **점프 필드 없음**: 지상 y=-1이 점프, 공중 y=-1은 각도만.
- **다이빙 = 지상 + 이동 + hit + y≠-1** 네 조건. y=-1이면 점프 후 파워히트로 바뀜.
- **반응 지연은 정상 속도에서 보통 약 1처리프레임(40ms), 새 결정 주기는 3프레임**: 데드밴드로 진동 완화. 느린 봇·슬로모션에서는 벽시계 지연이 더 늘어남.
- **파일은 Worker 생성당 1회 실행**: 최초 Apply 또는 그 side의 모드·파일이 바뀐 Apply에서 새 Worker를 만들며, 같은 설정의 Apply에서는 기존 Worker를 재사용. 전역은 틱 사이는 물론 세트·매치가 바뀌어도 유지되고, 설정 변경 Apply·강제 재시작 시 리셋됨. 양쪽에 같은 파일을 써도 Worker와 전역은 서로 분리.
- **expectedLandingPointX는 순간 예상값**, 캐시 금지.
- **state에 따라 입력 효과가 다름**: 2(파워히트 중)는 수평 이동이 가능하고 충돌 순간 x·y가 타구를 결정하며, 3(다이빙)·4(누움)는 현재 입력이 대부분 무시됨. `hit=1`을 계속 보내면 공중 재발동·착지 다이빙이 나감. "반응이 씹혔다" 싶으면 state 로그.
- **meta.score는 내 관점.**
- **매 틱 console.log 금지**: 카운터로 걸러서(예 100틱마다). 로그 위치는 F12 Console(JS·Python 동일).
- **Python**: 첫 로딩 수 초, dict 접근, 표준 라이브러리만(numpy 불가), 반환 dict 필수.

---

## 12. 당일 추가 스킬 — 가이드 + Q&A 통합

가이드:
- 대회 당일 신규 스킬이 공개되며 스냅샷 API가 확장될 수 있음. 기존 필드 불변, 새 필드만 추가.
- 스킬 대응 로직이 없어도 게임은 멈추지 않음(다만 불리).
- 스킬 공개 시 가이드와 API 문서가 함께 갱신됨.
- 수비형은 통하지 않을 가능성 높음 → 공격 권장.

Q&A로 확정된 사항:
- **히든 룰은 없고 스킬 추가만** 있음(인스타 공지의 "히든 룰 or 스킬"에서 변경).
- **스킬 공개 후 첫 경기까지 1시간.**
- 당일 **스킬이 포함된 새 레포지토리를 공개**하며 localhost에서 돌려 보며 코드를 수정할 수 있음.
- 새로 배포되는 내장 AI는 스킬을 못 쓰지만, **스킬을 사용할 수 있고 일정 수준 실력을 가진 봇 코드를 제공**할 예정.
- **스킬은 이론적으로 방어 가능**("충분히 가능합니다").
- 스냅샷의 기존 필드는 유지되고 몇 개 필드가 추가됨. 기존 스냅샷대로만 반환해도 스킬 사용을 제외한 나머지는 정상 동작.

---

## 13. Q&A 시트 전문 (2026-09-05 기준 11건)

시트 운영 안내: 질문 앞에 팀명 표기, 최대한 자세히, 대회 전날까지 매일 10:00~22:00 운영·운영 시간 내 최대 2시간 이내 답변, 질문·답변 삭제 금지. 예시 질문/답변: "대회 당일 파이썬 문법 구글 검색 가능?" → "당일 코드 수정 시 웹 검색 불가. 운영 사이트에 링크된 공식 문서 참고 가능."

| # | 질문 (팀) | 답변 |
|---|---|---|
| 1 | [럭키펀치] LLM 질문 3회 제한은 팀당인가 인당인가? | 팀당 3회, 회당 300자 제한. |
| 2 | [Avian Influenza] 당일 추가 조건이 decide 입력값(스냅샷)을 바꾸는가? | 기존 필드 유지, 몇 개 필드 추가 예정. 기존 스냅샷대로 반환해도 스킬 사용 제외 나머지는 정상 동작. |
| 3 | [섭지코지] 공이 머리 위에서 작게 튕기거나 머물다 캐릭터를 통과해 실점하는 현상은 코드로 방지해야 하는가, 운영진 판단인가? | 같은 조건에서 항상 발생하는 현상은 버그로 보지 않음. 게임의 일부로 실점 인정. |
| 4 | [섭지코지] 1) 스킬 공개 후 첫 경기까지 시간? 2) 스킬 반영 게임 코드를 localhost에서 받을 수 있나? | 1) 1시간. 2) 스킬 포함 새 레포지토리를 당일 공개, localhost에서 수정 가능. |
| 5 | [Avian Influenza] 새로 배포되는 파일의 내장 AI도 스킬을 쓰는가? | 새 AI는 스킬 못 씀. 대신 스킬을 쓰고 일정 수준 실력을 가진 봇 코드를 제공 예정. |
| 6 | LLM 질문 시 외부 링크를 넣을 수 있나? | 불가능. |
| 7 | [Avian Influenza] 히든 룰 없이 스킬만 추가로 변경된 건가? | 네, 스킬 추가만 있을 예정. |
| 8 | 경기는 한 PC에서 두 팀 봇 파일을 봇 설정 패널에 넣고 Apply 후 시작하는 방식인가? | 네 맞습니다. |
| 9 | 대회 PC에서 봇 응답이 다음 프레임 전에 도착하는지, 지연 빈도 실측값? | 예산 120ms, 보통 3ms 내 리턴. RAM 16GB, CPU 점유 낮춰 진행하여 사양 제한 없음(과반복 코드 없을 경우). |
| 10 | [Avian Influenza] 당일 Google Colab 사용도 제한되나? | 코랩 내장 AI 기능을 차단하고 현장 검사하여 형평성 문제 없으면 사용 가능. |
| 11 | 새 스킬은 이론적으로 방어 가능한 스킬인가? | 충분히 가능. |

### 13.1 Q&A에서 도출되는 당일 운영 규칙 (가이드에는 없음)

- 당일 코드 수정 시 **웹 검색 불가**. 운영 사이트에 링크된 공식 문서(MDN, Python docs)만 참고.
- **LLM 사용은 팀당 3회, 회당 300자**, 외부 링크 포함 불가.
- Google Colab은 내장 AI 차단 + 현장 검사 통과 시 사용 가능.
- 경기는 운영 PC 한 대에서 양 팀 파일을 봇 설정 패널에 넣고 Apply.
- 재현 가능한 엔진 현상(예: 머리 위 공 통과)은 버그가 아니라 게임의 일부 → 코드로 대비해야 함.

---

## 14. 부정행위로 간주되는 것

- 파일 최상위에서 매치와 무관한 대량 연산으로 초기화 타임아웃을 고의로 넘겨 상대 시스템 방해.
- 브라우저 취약점·엔진 버그 exploit(예: 물리 엔진에 NaN/Infinity 주입).
- 게임 코드·상대 봇 코드 탈취·수정, 온라인 기존 봇 표절.
- shaims 또는 리온이 배구 코드 악용 → 대회 불이익.
- 의도치 않은 버그는 부정행위 아님. "고의"만 해당. 헷갈리면 운영진 문의.

---

## 15. 우리 팀 시사점 (정리자 메모, 사이트 원문 아님)

- **5터치 규칙**: 저장소 엔진 `touchLimit.js`에 이미 구현(MAX_TOUCHES_PER_SIDE = 5). 시뮬·벤치와 실전 규칙 일치.
- **세트 4분 제한·골든볼·무승부**: 저장소 엔진에 없음(grep 결과 없음). 무한 랠리 교착이 나오면 앞서 있을 때는 시간이 내 편, 뒤질 때는 손해. 예선은 무승부도 승점에 반영되는 구조(순위표에 D 컬럼).
- **당일 시간**: 스킬 공개 → 첫 경기 1시간. 기존 `bot-dev/RUNBOOK_당일.md`는 3시간 기준으로 작성되어 있어 **1시간 버전으로 압축이 필요**. 또한 런북은 "LLM 없이"를 전제하지만 Q&A상 팀당 3회·300자 LLM 질문은 허용됨.
- **스킬 정보원**: 당일 새 레포지토리 + 스킬을 쓰는 제공 봇 코드. 제공 봇의 스킬 사용 로직을 읽는 것이 가장 빠른 규칙 파악 경로.
- **수비형 경고**: 운영진이 수비형은 스킬에 안 통할 가능성이 높다고 명시. 공격 루트 확보가 우선.
- **제출**: 09-05 기준 0건. 최신본만 사용되므로 안정 버전을 미리 한 번 올려두는 것이 안전.
- **Chrome 최신 버전**이 당일 중계 환경. 로컬 검증도 Chrome으로.

---

## 16. 링크

- 운영 사이트: https://shaims.vercel.app (홈 / /submissions / /brackets / /guide)
- 리온이 배구 저장소: https://github.com/SKKU-x-HYU-SW-Competition/leonyi-volleyball
- 원작: https://github.com/gorisanson/pikachu-volleyball
- Q&A 시트: https://docs.google.com/spreadsheets/d/17asbemRo6h4q4-rrH7_rFOJd5WEU4szJmqyxSJmbQjo/edit?usp=sharing
- 대회 영상 저장소(Google Drive, 본선 영상 업로드 예정): https://drive.google.com/drive/folders/195-QSo6zlsODJ7y3Za2gv57u5S_rQ909?usp=drive_link
- MDN JavaScript: https://developer.mozilla.org/ko/docs/Web/JavaScript
- Python 튜토리얼: https://docs.python.org/ko/3.14/tutorial/index.html
- 인스타그램: https://www.instagram.com/skku_hyu_cse
