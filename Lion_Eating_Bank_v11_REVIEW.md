# Lion_Eating_Bank_v11 최종 검증 보고서

- 검증 대상: `Lion_Eating_Bank_v11.js`
- SHA-256: `1018FECDFB1D5087514DADC03B448FE3329C898E516DEEF57A62237559FE8601`
- 크기: 2,541줄 / 185,241 bytes
- 검증 시각: 2026-09-05 01:16:49 KST
- 검증 기준: `COMPETITION_GUIDE.md`, 공식 저장소 main `1f3cecb90aca174ffc42ac6be4c384cc725d9e91`
- 검토 원칙: 원본 파일을 수정하지 않고 정적 전수 스캔, 계약 호출, 공식 엔진 대조, 브라우저 실행으로 검토

## 최종 판정

**조건부 제출 가능.**

현재 공식 엔진에서 파일 로드와 경기 완료에는 성공했다. 물리 상수, 파워히트, 수비·공격 시뮬레이션도 전반적으로 현재 엔진과 잘 맞는다. 다만 다음 두 문제를 구분해야 한다.

1. 현재 경기에도 영향을 줄 수 있는 문제: 새 랠리의 터치 카운터가 확실히 초기화되지 않는다.
2. 당일 스킬을 켤 때 발생하는 문제: `SK.fire`가 추가한 스킬 출력 필드가 최종 `sanitize`에서 삭제된다.

첫 번째 문제는 제출 전에 한 줄 조건을 수정하고 회귀 대결하는 것을 권장한다. 두 번째는 스킬 공개 후 `SK.fire=1`을 사용하기 전에 반드시 수정해야 한다.

## 주요 발견

| # | 영역 | 등급 | 파일:줄 | 문제 | 근거·재현 | 권장 조치 | 확신도 |
|---:|---|---|---|---|---|---|---|
| 1 | A5·A6 | 중요 | `Lion_Eating_Bank_v11.js:2084-2114`, `2265` | 새 랠리에서 `g_touches`, `g_touch_hist`, `g_tr_active`가 확실히 초기화되지 않음 | 코드는 `rallyFrameCount < 4`만 새 랠리로 보지만 실제 새 랠리 첫 관측값은 41~43이다. 인메모리 계측에서 이전 랠리 `g_touches=2`가 새 랠리 첫 스냅샷 `rfc=42`에서 `3`으로 남았다. 수정 조건을 적용하자 `0`이 됐다. | 2088에 `rfc < 4 || (g_adapt.lastRallyFrame !== null && rfc < g_adapt.lastRallyFrame)` 조건을 사용한다. 2265에도 같은 랠리 감소 조건을 반영하고 대결한다. | 높음 |
| 2 | A5·A6 | 중요 | `1510`, `1597`, `1885`, `1940`, `2128`, `2219-2231`, `2444` | 정확한 자체 물리 예측기가 있으면서 상위 분기에서 엔진의 부정확한 `expectedLandingPointX`를 반복 사용 | 실제 네트 상단 판정은 `y <= 192`, 공식 착지 예측기는 `y < 192`다. `(x=210,y=192,vx=10,vy=5)`에서 실제 착지는 `x=380/18프레임`, 엔진 예측은 `x=140/8프레임`으로 재현됐다. | 기존 `stepBall` 기반 로컬 착지 함수를 만들고 사용처를 교체하는 것이 정확하다. 출력 변화 범위가 넓으므로 충분한 대결 없이 전날 일괄 변경하지 않는다. | 높음 |
| 3 | B2 | 중요 | `177-182`, `2442`, `2524-2527` | `SK.fire=1`로 추가한 스킬 필드가 최종 출력에서 삭제됨 | `applySkill`은 `a[SK.key]=1`을 추가하지만 `sanitize`는 `{x,y,hit}`만 새로 만든다. 인메모리 재현에서 `skill:1`이 최종 객체에서 사라졌다. | 최종 줄을 `fin = applySkill(s, sanitize({...})) || pre` 순서로 바꿔 기본 필드를 먼저 정규화하고 스킬 필드는 보존한다. | 높음 |
| 4 | A5 | 중요 | `155`, `253-278` | `NEW_GAME_RESET=0`이라 서브 밴딧 상태가 다음 상대에게 이월될 수 있음 | Worker 전역 수명은 매치가 아니라 Apply-to-Apply다. 상대 파일만 바꾸면 우리 Worker와 `BC.stats/mode`는 유지된다. | 안전 우선이면 `NEW_GAME_RESET=1`. `0`을 유지하면 상대 변경 때 우리 Worker를 반드시 재생성한다. 동일 상대 다세트에서는 `0`이 학습 유지에 유리하다. | 높음 |
| 5 | A4 | 중요·조건부 | `328-345`, `1103-1118`, `2172`, `2279` | `tickFrameGroupSize`를 읽지만 모든 로직이 일반화되지는 않음 | Thunder는 값이 3이 아니면 안전하게 꺼지지만 FLY 단계 `3*h`, 재계획 단계와 TH-RETREAT 위상 계산은 3으로 고정됐다. | 현재 공식값 3에서는 변경하지 않는다. 값이 바뀌면 우선 `THUNDER_SERVE=0`, `EXP_REACH=0`, `TH_RETREAT=0`으로 안전화한다. | 높음 |
| 6 | A6 | 중요 | `2433-2534` | 4분 제한에 따른 앞섬/뒤짐 전략이 없음 | 스코어는 랠리 감지에만 사용하며 신뢰할 경기 경과시간 필드는 사용하지 않는다. 교착 상대에게 뒤지는 상황에서도 공격성이 바뀌지 않는다. | 정확한 시간 필드가 없는 상태에서 벽시계 의존 로직을 추가하지 않는다. 현재 `THUNDER_SERVE=1`을 유지하는 것이 교착 방지 안전책이다. | 중간 |
| 7 | B3 | 중요 | `139-159` | 공격 전체를 즉시 끄는 단일 안전 스위치가 없음 | `THUNDER_SERVE`는 Thunder만 끄며 FAST/KILL/PASS-JUMP는 별도 경로다. | 최후 안전화 절차를 `THUNDER_SERVE=0` + `KILL_GATE.MARGIN` 증가로 문서화한다. | 높음 |
| 8 | A9 | 중요·운영 | 파일명 | 현재 GitHub 파일명은 `Lion_Eating_Bank_v11.js` | 사이트 제출은 팀명 파일을 받아 버전을 붙이고 로컬 UI는 `<팀명>_v<N>.js` 형식을 사용한다. | 사이트 업로드본 `사자먹는은행.js`, 로컬 검증본 `사자먹는은행_v11.js`를 준비한다. | 높음 |
| 9 | B5·B6·B7 | 중요 | `Lion_Eating_Bank_v11.md:3,33,44,73` | 문서가 참조하는 `bot-dev/` 도구와 원본 결과가 이 저장소에 없음 | 사용자 저장소, 공식 저장소, 검토 워크스페이스에서 `RUNBOOK_당일.md`, `CODE_MAP.md`, `eval_skill.mjs`와 결과 JSON을 찾지 못했다. | 실제 비공개 검증 저장소의 존재와 최신성을 대회 전에 확인한다. | 높음 |
| 10 | C2 | 정리 | `140` | `DEBUG=true` | 매 틱 로그는 아니지만 서브 발동·포기·모드 전환에서 문자열 조립과 로그가 발생한다. | 스킬 필드 확인이 끝난 최종 제출본은 `false`로 바꾼다. | 높음 |
| 11 | C1 | 정리 | `1833-1837` | `jumpYAt`가 호출되지 않음 | 정의 외 참조가 없고 삭제본과 원본의 2,000입력 차분에서 출력 차이가 0이었다. | 대회 전날에는 삭제하지 않아도 된다. | 높음 |

## 기준별 결론

### 1. 효율적인 코드인가

적합하다.

- 모든 주요 탐색 루프에 상한이 있다.
- 최대 킬 점프 탐색은 약 `270회 × 44프레임` 미시뮬레이션으로 제한된다.
- `BC.log=120`, `M.rallies=400`, 터치·최근 패턴·지연 기록도 각각 상한이 있다.
- 2,000개 계약 입력 호출에서 잘못된 반환 0, 내부 예외 0이었다.
- v11 문서에는 실제 Chrome p99 12.7~14ms, 최대 93ms라고 기록돼 있다. 다만 원본 결과 JSON이 저장소에 없어 이 수치는 독립 재현하지 못했다.

### 2. 가능한 대부분의 경우에 작동하는가

예외 보호와 출력 정규화는 좋다.

- 최상위 `decide(snapshot)`이 존재한다.
- 정상 경로와 예외 경로 모두 `{x,y,hit}`를 반환한다.
- `sanitize`가 방향을 `-1/0/1`, hit를 `0/1`로 제한한다.
- 오케스트레이터, Thunder, AC, 스킬 각각 예외 보호가 있다.
- `fetch`, XHR, Worker 생성, import, require, DOM, 벽시계 의존 로직이 없다.
- 모르는 스냅샷 필드가 추가돼도 기존 로직은 유지된다.

예외는 새 랠리 터치 누수와 네트 `y=192` 착지 예측 경계다.

### 3. 수직 썬더를 잘 방어하는가

반복되는 이탈 평타형 썬더에는 구조적으로 강하다.

`TH_RETREAT_ADAPT=2`는 해당 터치 순번에서 깊은 이탈 평타가 관측된 뒤에만 후퇴한다. 따라서 처음 보는 위상은 의도적으로 내줄 수 있지만, 한 번 학습한 뒤에는 이탈 평타 리시브 위치로 이동한다. 이는 비이탈 급강하 서버를 상대로 불필요하게 물러나는 손실을 피하기 위한 선택이다.

중요하게도 `MAX_BLEND=0`은 이 기능을 끄지 않는다. `MAX_BLEND`는 일반 수비 위치 EMA 편향이고, 수직 썬더 적응은 별도의 `g_nt`와 `TH_RETREAT_ADAPT`를 사용한다.

### 4. 득점 포인트를 놓치지 않는가

공격 구조는 합리적이지만 의도적으로 보수적이다.

- 빠른 공격과 FLY-3 킬 후보를 먼저 찾는다.
- 상대가 못 받는다고 판정된 공격만 점프 스매시로 채택한다.
- 확정킬이 아니면 안전한 패스와 수비를 우선한다.
- `KILL_MAX_CONTACT=16` 때문에 먼 미래의 잠재 공격은 포기한다.

이는 실수를 줄이는 설계지만, 착지 예측 경계와 잘못 이월된 터치 수가 득점 후보를 부당하게 제거할 수 있다.

### 5. 주어진 물리엔진과 일치하는가

주요 상수와 계산은 현재 공식 엔진과 일치한다.

| 물리 항목 | 값 |
|---|---:|
| 코트 폭 / 네트 x | 432 / 216 |
| 플레이어 지상 y / 반폭 | 244 / 32 |
| 공 착지 y | 252 |
| 공 y속도 상한 | ±40 |
| 네트 반폭 / 상단 구간 | 25 / 176~192 |
| 걷기 / 다이빙 속도 | 6 / 8 |
| 점프 초기속도 / 중력 | -16 / +1 |
| 파워히트 x속도 | 10 또는 20 |
| 파워히트 y속도 | `max(15, abs(vy)) × y입력 × 2` |
| 정상 적용 지연 / 결정 그룹 | 1프레임 / 3프레임 |

v11의 `stepBall`은 실제 엔진의 `y <= 192` 네트 판정을 정확히 사용한다. 문제는 정확한 자체 계산과 부정확한 엔진 제공 착지점을 혼용한다는 점이다.

### 6. 물리엔진 정보를 잘 활용하는가

매우 적극적으로 활용한다.

- 플레이어 state와 frameNumber
- 점프·파워히트 상태 전이
- 다이빙 첫 프레임 걷기 6px와 이후 8px 이동
- 누운 상태의 접촉 가능성
- 실제 충돌 프레임의 x/y 입력
- 상대 반응 지연과 도달 범위
- 네트·벽·천장 반사
- 연속 터치 예산

다만 일부 상위 분기에서 `expectedLandingPointX`를 그대로 읽는 부분은 개선 여지가 있다.

## MAX_BLEND 검토

현재 `MAX_BLEND=0`을 유지하는 것이 타당하다.

- 기본 수비는 이미 표준 위협 전체의 최소최대 방어점을 계산한다.
- EMA를 혼합하면 반복 패턴에는 유리할 수 있지만 미지 상대와 페이크에는 약해질 수 있다.
- 현재 코드는 3회 관측 이후부터 신뢰도를 계산하고 12회에서 최대 신뢰도에 도달한다. 10점 한 세트에서는 충분한 표본을 얻지 못할 수 있다.
- 코드 주석상 미지 상대 풀 평균은 OFF가 유리하다.
- `0.62`는 반복 착지 코스가 실제로 확인된 상대에게만 사용할 후속 세트용 후보로 둔다.
- 측정되지 않은 0.1~0.4 중간값은 사용하지 않는다.

## 당일 노브 전수검토

| 노브 | 현재값 | 안전 범위·판정 |
|---|---:|---|
| `DEBUG` | `true` | 조사 중 true, 제출 false |
| `LAT_GUARD` | `1` | 0/1 |
| `LAT_WINDOW` | `30` | 양의 정수. 주석에 안전 범위가 부족함 |
| `SELF_CHECK` | `1` | 0/1 |
| `SELF_CHECK_TOL` | `2` | 0 이상 픽셀 |
| `CAMP_ABORT_X` | `0` | 기본 0, 확인된 네트 캠퍼에만 262 |
| `THUNDER_SERVE` | `1` | 0/1. 현재 1 유지 |
| `SERVE_BANDIT` | `1` | 0/1 |
| `BANDIT_BLOCKWIN` | `0.5` | 0~1 |
| `BANDIT_MODES` | 문자열 | `thunder`, `steep`, `ac`, `flat`만 사용 |
| `CAMP_ESCAPE` | `1` | 0~3 |
| `AC_LOSS_COMEBACK` | `1` | 0/1 |
| `NEW_GAME_RESET` | `0` | 안전 기본 1, 동일 상대 다세트 학습 유지 시 0 |
| `EXP_DRIFT_ATTACK` | `1` | 0/1/2 |
| `EXP_S2_TRACK` | `1` | 0/1 |
| `EXP_REACH` | `1` | 0/1. 물리 변화 시 긴급 0 |
| `MAX_BLEND` | `0` | 기본 0, 반복 상대가 확인된 경우만 0.62 후보 |

## 당일 1시간 절차

### 장애별 손 절차

1. 낙하점이 틀림
   - 제공 스킬 봇의 공 갱신 순서를 확인한다.
   - `stepBall`의 속도 상한, 중력, 네트 처리를 수정한다.
   - `expectedLandingPointX` 사용처를 점검한다.
   - 좌우 한 경기씩 실행한다.

2. 공이 갑자기 빨라지거나 궤적이 변경됨
   - `BALL_MAX_Y_VELOCITY`와 파워히트 식부터 확인한다.
   - Thunder 자기검증이 반복 발동하면 `THUNDER_SERVE=0`으로 전환한다.
   - 장기 예측이 틀리면 `EXP_REACH=0`으로 안전화한다.

3. 공격이 막히거나 되받아침
   - 네트 캠퍼가 확인되면 `CAMP_ABORT_X=262`를 시험한다.
   - `CAMP_ESCAPE=1`과 밴딧 전환 로그를 확인한다.
   - 모든 Thunder가 무효면 `THUNDER_SERVE=0`으로 전환한다.

4. 특정 행동이 규칙상 실점
   - `applySkill` 또는 최종 출력 직전에서 해당 행동을 중립화한다.
   - 원인을 모르면 `SK.fire=0`을 유지한다.

5. 상대 순간이동·크기 변화
   - 전체 `PLAYER_HALF`나 `WALK_SPEED`부터 바꾸지 않는다.
   - `EXP_REACH=0`, `KILL_GATE.MARGIN` 증가, Thunder OFF 순서로 보수화한다.

### 시간표

- 0~5분: v11 해시 확인, 안정 제출본 별도 보관
- 5~12분: 새 저장소와 제공 스킬 봇에서 필드·발동조건·효과·지속시간 추출
- 12~20분: `DEBUG=true`로 새 필드 확인, `SK` 매핑과 출력 보존 수정
- 20~32분: 제공 스킬 봇 상대 좌우 1경기씩
- 32~42분: 필요한 최소 한 줄 수정만 적용
- 42~52분: 좌우 재경기와 기존 안정 상대 회귀 경기
- 52~56분: `DEBUG=false`, SHA-256, 크기, 로드 확인
- 56~60분: `사자먹는은행.js` 업로드 및 사이트 최신 버전 확인

### LLM 질문 3회 제안

1. `다음 스킬 구현에서 입력 필드, 발동조건, 지속시간, 공·플레이어 물리 변경 순서를 표로 추출해줘. 추측하지 말고 코드 줄 기준으로 답해줘.`
2. `이 스킬을 v11의 applySkill 한 곳에 최소 수정으로 넣을 때 필요한 변경과 기존 x/y/hit를 깨뜨릴 위험을 검토해줘.`
3. `수정 diff에서 무효 반환, 스킬 필드 삭제, 5터치, 좌우 비대칭, 360ms 초과 가능성만 최종 감사해줘.`

## v11 코드 지도

| 구간 | 역할 | 당일 수정 시점 |
|---|---|---|
| 139~183 | 당일 노브·스킬 어댑터 | 스킬 필드와 발동 조건 |
| 189~404 | Thunder 서브·밴딧·이탈 | 서브가 막힐 때 |
| 413~545 | 공·자기 물리 시뮬레이션 | 물리 상수 변경 |
| 561~1295 | 확정킬·상대 도달·공중 공격 | 상대 강화·크기 변경 |
| 1319~1627 | 상대 공격 열거·수비 위치 | 새로운 공격 궤적 |
| 1632~1832 | 지상 패스·서브·점프 패스 | 터치·패스 규칙 변경 |
| 1849~2114 | 상대 학습·TH 패턴·터치 추적 | 반복 패턴·새 랠리 감지 |
| 2133~2387 | 본체 의사결정 | 최후에만 수정 |
| 2398~2427 | AC 예외 보호·상태 동기화 | 예외 발생 시 |
| 2429~2541 | 오케스트레이터·검증·스킬 출력 | 새 필드·출력 계약 |

## 삭제 후보

- `jumpYAt` 1833~1837: 정적으로 완전 미사용이며 2,000입력 차분에서 삭제 전후 출력이 같았다.
- `DEBUG` 관련 코드는 삭제하지 않고 최종값만 false로 둔다.
- `NET_TOP`, `DIVE_LATE`, `DIVE_HAILMARY`, `PJ_SIM`, `PASS_DIG`, `SERVE_JUMP`, `MAX_BLEND` 등 비활성 분기는 당일 복구 노브이므로 지금 삭제하지 않는다.
- v1~v10 파일은 제출물에 포함하지 않되 대회 전에는 기록으로 유지한다.

## 브라우저 직접검증

- 공식 저장소 main에 SHA-256이 동일한 v11을 넣어 로컬 브라우저에서 실행했다.
- `v11 LEFT` 대 `v10 RIGHT` 경기는 **10:3으로 정상 종료**했다.
- 종료 후 페이지 error/warn 로그는 0건이었다.
- 로드 실패, Worker 재시작, 무효응답 표시는 없었다.
- 약 120ms 간격의 연속 화면에서 상대 서브에 대해 v11이 네트 근처에서 뒤로 이동하는 수비 동작을 확인했다.
- 일부 화면 캡처는 렌더 프레임이 중복됐기 때문에 이 관찰만으로 TH-RETREAT의 내부 1~2스냅샷 타이밍을 정량 증명하지는 않는다.
- 한 경기는 실행 가능성 증거이며 모든 미지 상대 대비 SOTA의 증명은 아니다.

## 제출 전 권장 순서

1. 새 랠리 터치·후퇴 상태 초기화 조건 수정
2. 현재 v11과 대결 및 좌우 대칭 검사
3. `NEW_GAME_RESET=1` 또는 상대 변경 시 Worker 재생성 절차 확정
4. 최종본 `DEBUG=false`
5. 로컬 파일명 `사자먹는은행_v11.js`, 사이트 업로드 파일명 `사자먹는은행.js`
6. 당일 스킬 사용 시 스킬 필드가 sanitize 뒤에도 남는지 단위 확인

## 확인하지 못한 것

- 실제 대회용 최신 Chrome이 아니라 Codex 인앱 Chromium 브라우저에서 실행했다.
- 문서가 인용하는 `bot-dev/rule_check.mjs`, Chrome 결과 JSON, `eval_skill.mjs`, `CODE_MAP.md`, `RUNBOOK_당일.md`가 제공 저장소에 없어 독립 실행하지 못했다.
- 검토 워크스페이스의 물리엔진은 최신 공식 ±40 변경 전 버전이므로 기존 로컬 duel 수치를 이번 공식 엔진 증거로 사용하지 않았다.
- 공개되지 않은 당일 스킬과 타 팀 봇은 검증하지 못했다.
- 40ms 엔진 프레임별 Worker 내부 상태와 TH-RETREAT 발동 카운터는 브라우저 UI에서 직접 계측하지 못했다.
- `Lion_Eating_Bank_v11.md`의 24시드 표가 ADAPT 1 결과이고 최종값은 ADAPT 2라는 설명은 확인했지만 원본 결과 파일이 없어 수치를 재검산하지 못했다.
