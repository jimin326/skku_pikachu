# Lion_Eating_Bank_v6_Defense — 수비형 재설계

v5_1(썬더 서브)은 그대로 두고, **지상 판단부 전체를 물리 기반 수비 상태기로 교체**한 버전.
`v6dev/build_v6.mjs` 가 `origin/main:Lion_Eating_Bank_v5_1.js` + `v6dev/gd_module.js` 에서 생성한다.
**제출 파일을 손으로 고치지 말 것.** 고칠 곳은 `gd_module.js` / `build_v6.mjs` 뿐이다.

```
node v6dev/build_v6.mjs ./Lion_Eating_Bank_v6_Defense.js ./v6dev/base_v5_1.js
```

---

## 0. 왜 v5 의 수비가 0% 였는가

강한 봇끼리 붙이면 **서브권자가 100% 이긴다**. 즉 유일하게 의미 있는 지표는
"상대 서브(썬더) 랠리를 몇 % 살려내는가"이고, v5_1 은 그 값이 **정확히 0/570** 이었다.

원인을 실측으로 분해했다(`v6dev/diag_notouch.mjs`). 무접촉 실점 41건이 **전부 같은 한 장면**이었다.

```
상대 스파이크 (224,171) v(-20,38)
  → 우리 코트 프레임 1: (204,209)   지상 y 창 [212,252] 밖 (3px 차이로 못 닿음)
  → 우리 코트 프레임 2: (184,248)   ★ 유일한 접촉 가능 프레임
  → 프레임 3: y=288 > 252 → 실점
우리 위치: x=126~132   필요: x ≥ 152
```

즉 v5 는 "낙하지점(ELP=116)을 추종"하다가, **실제로 존재하는 단 한 프레임의 접촉창**에서
20~26px 벗어나 있었다. 낙하지점 추종이라는 문제 설정 자체가 틀렸다.

---

## 1. 물리 엔진에서 확정한 사실 (전부 `physics.js` 유도)

| # | 내용 |
|---|---|
| **P1** | 충돌 조건은 `|bx-px| ≤ 32 && |by-py| ≤ 32` (FUN_00403070). 지상 `py=244` → 접촉 가능 `by ∈ [212,276]` |
| **P2** | 그러나 `by+vy > 252` 이면 **충돌 판정보다 먼저** 실점이 확정된다. 실효 접촉창은 `by ∈ [212,252]` 이고, 빠른 공에선 **1프레임** |
| **P3** | 비파워 반사 `xV = sign(bx−px)·⌊|bx−px|/3⌋`, `yV = −max(15,|yV|)`. **낙하지점 정중앙에 서면 `xV≈0`** — 공이 제자리에 뜬다. 네트로 보내려면 공보다 18~32px 뒤 |
| **P4** | 네트 기둥: `|bx−216| < 25 && by > 192` 이면 우리 쪽 공은 `xV = −|xV|` 로 **되돌아온다** (`x ∈ (191,241)` 밴드) |
| **P5** | 스냅샷은 프레임 f₀. 이번 결정은 **f₀+2 부터** 3프레임 단위로 먹는다(f₀+1 은 직전 액션). → 도달 가능 위치는 **18px 격자**. "낙하지점에 정확히 선다"는 원리적으로 불가능 |
| **P6** | 다이빙: x 는 13프레임 동안 `6+8×12 = 102px` 강제 이동, y 는 최소 229까지 상승(접촉창 `[197,261]` 로 확대). 대신 착지+lyingDown 까지 **총 18프레임 무제어** |
| **P7** | 점프: 33프레임(=11틱) 구속. 그 사이 어떤 재계획도 불가능 |

`gd_module.js` 의 `stepBall`/충돌/반사는 실엔진과 **비트 단위 일치**를 검증했다
(`v6dev/verify_gd.mjs`: 자유비행 55,823프레임 · 접촉 4,000케이스 · 반사속도 — **불일치 0**).

---

## 2. 판단 구조

```
공이 우리 코트에 떨어지고 상대가 더 못 건드림
    → planReceive : 실제 컨트롤러를 그대로 돌린 co-simulation 으로
                    (접촉 프레임 × 접촉 위치)를 전수 탐색, 반사 후 궤적까지 평가
그 외 전부
    → planStandby : 상대의 가능한 타격 결과 전체에 대한 가중 minimax 대기 위치
```

### planReceive — 목표는 좌표가 아니라 "접촉"
목표 x 를 `minX..maxX` 전수로 놓고, 각 목표에 대해 **우리가 실제로 쓸 컨트롤러**
(3프레임 묶음, 18px 격자, 1프레임 지연)를 그대로 돌려 첫 접촉 프레임을 찾는다.
그 접촉의 반사속도를 [P3] 로 계산하고 **반사 후 궤적을 끝까지 전개**해 채점한다:
네트를 넘기는가 / 얼마나 깊은가 / 상대가 닿을 수 있는가 / 못 넘기면 두 번째 기회가 있는가.
[P4] 네트 되돌림 함정은 궤적을 실제로 굴려보므로 **자동으로 회피된다**.

걷기로 접촉이 불가능할 때만 다이빙 후보를 연다. 다이빙은 [P6] 무제어 18프레임을
점수에서 직접 차감하므로, "굳이 안 해도 되는 슬라이딩"은 나오지 않는다. → **요구 1·2**

### planStandby — 안전한 위치가 1순위
1. 상대가 **언제** 칠지도 상대의 선택이다. 물리적으로 가능한 접촉 프레임 전체에서
   최대 `CMAX=8` 개를 균등 추출한다(단일 예측은 매 틱 크게 흔들려 목표가 진동한다).
   상대의 세로 도달 범위는 실제 점프 궤적 `y(m)=244−16m+m(m−1)/2` 로 계산한다.
2. 각 접촉 가설에서 파워히트 6종 + 비파워 3종을 전개한다.
3. **클래스 정규화**: 느린 공은 열거 변형이 많아 그냥 세면 확률이 부풀고 내리꽂기가 항상 밀린다.
   같은 종류(내리꽂기/수평/아치/리시브)끼리 가중치를 나눠 클래스 사전확률을 보존한다.
4. **치명도 가중**: 접촉 가능 프레임이 1개면 ×2.6, 2개면 ×2.0. 물리적으로 좁은 창이 곧 위험이다.
5. **최소 선행 지평(ANTICIPATE=12)**: 타격이 코앞이면 모든 후보 위치의 커버리지가 같아져
   목표가 부차항만 보고 진동한다. "최소 12프레임 뒤에 온다"고 보고 평가해야
   **미리 그 자리에 가 있게 만드는 기울기**가 생긴다.
6. 각 후보 목표에 대해 우리 위치 궤적을 실제로 시뮬레이션하고,
   `margin = 32 + reach − |접촉x − 내위치|` 를 걷기·슬라이딩 양쪽으로 계산해
   커버 개수를 1순위, 여유를 2순위로 최대화한다. → **요구 4**

### 중립 위치 — "아무 위협도 없는 시간"을 버리지 않는다
우리가 막 넘긴 직후처럼 위협이 하나도 없는 구간이 랠리마다 20~30프레임씩 있다.
구버전은 여기서 직전 목표를 유지하며 **가만히 서 있었고**, 그 시간이 곧 다음 스파이크에
못 가는 이유였다. v6 는 이때 **표준 최악 위협**(네트 바로 앞 `y≈171` 에서의 내리꽂기·수평타·아치)
으로 같은 최적화를 풀어 중립 위치를 유도한다 → LEFT `x=182` / RIGHT `x=250` (좌우 대칭).
이 한 가지 수정이 종합 61.6% → 70.2% 였다. → **요구 5**

### 점프 — 공격 셋업 전용
```
점프 허용 = 지상 수비 계획이 이미 성립  AND  접촉까지 ≥14프레임 여유
          AND  들어오는 공이 강타가 아님 (!isPowerHit && |xV| ≤ 12)
          AND  시뮬레이션이 상대의 대응 불가를 인증
```
`!isPowerHit` 조건이 핵심이다. v5 는 **상대 강타가 날아오는 중에 점프**해 33프레임 구속에
걸렸고, 그동안 깊은 공을 그대로 흘렸다(실측 트레이스 f269~f288). → **요구 3**

공격을 아예 끄면 리시브 승률이 10%까지 떨어진다(랠리를 끝낼 수단이 없다).
**지상에서 살아남고, 인증된 점프로만 마무리한다** 는 구성이 실제로 작동한다.

### 리시브 공격성 밴딧
팔 0=SAFE(인증 기준 700) / 팔 1=BOLD(350). 리시브 랠리 승패만으로 `(승+1)/(시도+2)` 를
비교해 온라인 선택한다. 관찰은 `meta.score` 뿐이고 라플라스 사전분포가 탐색을 만든다.
랠리가 `GD_LONG_RALLY=420` 프레임을 넘으면 기준을 점진적으로 낮춘다
(아무도 못 끝내는 랠리는 지는 것과 같다 — 94초짜리 무한 랠리를 이걸로 제거했다).

---

## 3. 성능 (실게임 상태머신 `sim_real`, 10점 경기, 6상대 × 좌우 × 5시드 = 60경기)

**튜닝에 쓰지 않은 시드 세트 `[5501,6203,7013,8117,9203]` 교차검증:**

| | v5_1 (원본) | **v6_Defense** |
|---|---|---|
| 종합 득점률 | 55.8% | **76.0%** |
| **리시브 승률** | 19.3% | **67.0%** |
| 서브 승률(썬더) | 100.0% | **100.0%** (213/213) |

상대별 리시브 승률:

| 상대 | v5_1 | v6 |
|---|---|---|
| v5_1 | 0.0% | **32.6%** |
| v5 | 0.0% | 0.0% |
| v4 | 0.0% | **78.9%** |
| v3 | 0.0% | **90.5%** |
| v2 | 63.2% | **100.0%** |
| saja | 52.6% | **100.0%** |

썬더 서브는 **한 점도 잃지 않았다**. 판단 비용 p50 13.6µs / p99 129µs / 최악 318µs
(실게임 예산은 3프레임당 1회 ≈ 120ms). 이상 스냅샷 5종에서 예외 누출 0.

### 개선 단계별 기여 (같은 미사용 시드 세트)

| 단계 | 종합 | 리시브 |
|---|---|---|
| v5_1 원본 | 55.8% | 19.3% |
| + 수비 상태기 (§1·§2) | 67.8% | 50.4% |
| + 2수 게임 트리 (§7) | 68.3% | 51.6% |
| + 공격 게이트 복원 (§8) | 71.9% | 59.3% |
| + 공격 탐색 완화 (§8) | **76.0%** | **67.0%** |

### 남은 약점: v5 (0%)
v5 는 같은 접촉점 `(224,171)` 에서 **내리꽂기 `v(−20,38)`** 와 **수평 강타 `v(−20,0)`** 를 섞는다.
`v6dev/truth.mjs` 로 전수 확인한 결과 이 둘은 **필요한 대기 위치가 배타적**이다:

```
p ≥ 172  → 6코스 중 5코스 방어 (스파이크 둘 다 O, 수평 강타 X)
p ≤ 168  → 6코스 중 4코스 방어 (스파이크 둘 다 X)
```

어떤 단일 위치도 둘 다 막지 못한다 — 이건 튜닝 문제가 아니라 코트 기하의 결론이다.

관측 빈도를 따라가는 적응(`clsMul`)도 구현해 실측했지만 **오히려 나빠졌다**:
종합 69.9%(OFF) → 65.9%(네트 40px 이내 타격만 학습) → 62.7%(70px) → 57.5%(전체).
이유는 게임이론적이다 — 배타적 선택지에서 관측 빈도를 좇는 것은 fictitious play 이고,
상대가 코스를 섞으면 "지금 안 막는 쪽"으로 그대로 얻어맞는다.
**치명도 가중 고정 사전확률(= minimax 헤지)이 더 강했다.** 코드는 남겨두고 기본 OFF
(`W.CLS_MIN = 9999`). 코스가 한쪽으로 고정된 상대가 확인되면 3 정도로 낮춰 켠다.

다음 단계로 유망한 방향은 **위치 선정이 아니라 반환 배치**다. 상대가 `(224,171)` 에서 치게
두지 말고 우리 리턴을 상대 뒷코트로 깊게 보내면, 거기서 나오는 스파이크는 훨씬 덜 치명적이다.
`scoreContact` 에 깊이 보상은 이미 있으나 아직 지배적 항이 아니다.

---

## 4. 노브 (`§0-GD`, 제출 파일 상단)

| 노브 | 기본 | 의미 |
|---|---|---|
| `GD_ON` | 1 | 0 이면 v5_1 원본 판단으로 롤백(`legacyGroundDecision` 보존) |
| `GD_ATTACK` | 1 | 0 이면 완전 무점프(썬더 서브 제외) |
| `GD_ATTACK_PH` | **1** | 상대 파워히트 공도 공격 대상 허용(§8). 0 이면 v6 초기 동작 |
| `GD_ATTACK_VMAX` | 12 | 이보다 빠른 공엔 절대 점프 금지 — **v5 실패의 실질적 방어선** |
| `GD_ATTACK_MIN_K` | 14 | 접촉까지 이만큼 여유가 있어야 점프 검토 |
| `GD_ATTACK_SCORE` / `_BOLD` | 700 / 350 | 리시브 밴딧 두 팔의 인증 점수 하한 |
| `GD_ATTACK_COOL` | 24 | 재점프 금지 프레임 |
| `GD_LONG_RALLY` | 420 | 초과 시 인증 기준 점진 완화(무한 랠리 방지) |
| `GD_RD_BANDIT` | 1 | 리시브 공격성 밴딧 |
| `GD_SERVE_GUARD` | 0 | 상대 서브 구간 가드 고정. 실측 역효과라 OFF(§6.3) |
| `GD_CLS_NEARNET` | 70 | 코스 학습 대상 범위(학습 자체는 `W.CLS_MIN` 으로 OFF) |

수비 사전확률·모델 상수는 `gd_module.js` 의 `W` 에 모여 있고 빌드 시 JSON 으로 덮어쓴다:

```
node v6dev/build_v6.mjs ./out.js ./v6dev/base_v5_1.js '{"TWOPLY":0,"CMAX":12}'
```

주요 `W` 항목: `TWOPLY`(2수 가중, 1000) · `ANTICIPATE`(선행 지평, 12) · `CMAX`(상대 접촉
시점 가설 수, 8) · `DOWN`/`FLAT`/`LOB`/`SOFT`(코스 사전확률) · `LETHAL1/2/4`(접촉창 치명도) ·
`CANON`(표준 위협 앵커) · `STICKY`(목표 히스테리시스) · `DIVE_MG`(다이빙 감점).

실측 역효과로 기본 OFF 인 것들(코드는 보존): `PUSH_DEEP` `GIVE_TH` `THREAT`(1수 배치, §6.3)
· `CLS_MIN`(코스 빈도 학습, §3) · `TIER1`(사전식 우선순위) · `EXACT_REACT` · `GUARD_PULL`.

## 5. 검증 도구 (`v6dev/`)

| 파일 | 용도 |
|---|---|
| `verify_gd.mjs` | GD 물리 모델 ↔ 실엔진 비트 일치 검증 |
| `truth.mjs` | 특정 타격점에서 대기 위치별로 **실제로 받을 수 있는 코스 수** 전수 계산 |
| `diag_notouch.mjs` | 무접촉 실점의 원인 분해(접촉창 없음 / 위치 선정 실패 / 이미 도달 불가) |
| `match.mjs`, `peropp.mjs` | 실게임 상태머신 경기 단위 평가 |
| `probe_thunder.mjs` | 썬더 리시브 프레임 단위 트레이스 |
| `sweep.mjs`, `msweep.mjs`, `par.sh` | 사전확률 그리드 탐색 |

---

## 6. 썬더 차단 심층 조사 (서브 썬더 + 랠리 중 썬더)

### 6.1 두 썬더는 같은 물리 객체다
공식 엔진에 별도의 "썬더 스킬"은 **없다**(`grep -i thunder src/` → `physics.js` 주석뿐).
썬더는 녹화 입력이 만들어내는 **창발적 기술**이고, 물리적 실체는 하나다:

> **네트 바로 앞(x≈217~238)에서 나오는 최대 낙하속도 파워히트.**
> 접촉 직후 `yV = max(15,|yV|)×2` 이므로 |yV| 30~40 이 나오고,
> 우리 코트의 지상 접촉 가능 프레임이 **1개**로 줄어든다.

서브 썬더는 서브 시퀀스 끝에서, 랠리 중 썬더는 우리 리턴 뒤에서 나올 뿐이다.

### 6.2 실측 분해 (`v6dev/diag_thunder.mjs`, 4상대 × 좌우 × 3시드)

```
리시브 72/180 (40.0%)
  실점: 서브 썬더 14 | 랠리 중 썬더 17 | 그 외 77
우리 리턴 뒤 상대 타격 지점의 네트 거리: 중앙값 113px, 네트앞(≤48px) 헌납 24.9%
```

썬더급 실점의 공통점 — **전부 지상(st0, y=244)에 있었고, 2~20px 모자랐다.**

| 국면 | 상대 타격 | 우리 x | 필요 x |
|---|---|---|---|
| 서브 | b(224,167) vy30 vx−20 | 126 | ≥152 |
| 랠리 | b(217,172) vy30 vx−10 | 158 | ≥165 |
| 랠리 | b(220,165) vy30 vx−10 | 166 | ≥168 |
| 랠리 | b(238,102) vy30 vx−10 | 146 | ≥166 |

즉 점프 오판이 아니라 **순수한 위치 문제**이고, 필요한 자리는 항상 네트 앞 끝이다.

### 6.3 시도한 8가지와 결과 — 전부 같은 Pareto 프론티어

| # | 개입 | 리시브 | 서브썬더 | 랠리썬더 | 그 외 |
|---|---|---|---|---|---|
| 0 | **현행 (채택)** | **40.0%** | 14 | 17 | 77 |
| 1 | 리턴 배치: 상대 접촉을 깊게(`PUSH_DEEP`) | 34.4% | 13 | 27 | 78 |
| 2 | 리턴 배치: 네트앞 헌납 감점(`GIVE_TH`) | 38.3% | 12 | 24 | 75 |
| 3 | 리턴 배치: 상대 최악 강요창(`THREAT`) | 19.4% | 9 | 38 | 98 |
| 4 | 배치 3항 전부 + 상대 위치 외삽(`OPP_EXTRAP`) | 22.8% | 6 | 46 | 87 |
| 5 | 1프레임 위협 사전식 최우선(`TIER1`) | 30.0% | 9 | 42 | 75 |
| 6 | 상대 서브 구간 가드 고정(`GD_SERVE_GUARD`) | 25.6% | 14 | 37 | 83 |
| 7 | 반응 지연 상수 완화(`REACT` 4→3) | 23.9% | **6** | 20 | 111 |
| 8 | 위상 기반 정확 반응 지연(`EXACT_REACT`) | 13.3% | **4** | 51 | 101 |

**썬더 실점은 얼마든지 줄일 수 있다(14→4). 그런데 줄인 만큼 다른 데서 더 잃는다.**
7·8번은 앞으로 더 나가 썬더를 막고 깊은 공을 통째로 버리는 것이고,
1~4번은 리턴을 "상대가 못 온다고 오판한" 낮고 빠른 드라이브로 몰아 오히려 네트앞
헌납률을 24.9% → 41~46% 로 **올렸다**.

### 6.4 왜 트레이드오프가 강제되는가 (기하학적 결론)

`v6dev/truth.mjs` 로 상대 타격점 (224,171) 의 6코스를 대기 위치별 전수 검증했다.

```
p ≥ 172  → 6코스 중 5코스 방어 (네트앞 스파이크 둘 다 O, 수평 강타 X)
p ≤ 168  → 6코스 중 4코스 방어 (네트앞 스파이크 둘 다 X)
```

* 내리꽂기 `v(−20,38)` → 접촉창 **1프레임**, x=184 → `p ≥ 152` 필요
* 수평 강타 `v(−20,0)` → 접촉창 k10~13, x=24~44 → 슬라이딩으로도 `p ≤ 144` 필요

두 조건은 **교집합이 없다.** 슬라이딩이 102px 를 커버해도 수평 강타의 접촉창은
13프레임 뒤라 다이빙 이동이 완주하지 못한다(앞선 손계산의 오류를 전수 검증으로 정정).
**어떤 단일 대기 위치도 둘 다 막을 수 없다** — 튜닝이 아니라 코트 기하의 결론이다.

### 6.5 실제 경기로 검증 (썬더 봇 상대, 10점 경기)

진단 카운터가 아니라 **경기 결과**로 프론티어 점들을 다시 재보면 결론이 뒤집힌다.

| 설정 | 종합 | 리시브 | vs v5_1 리시브 | 서브 |
|---|---|---|---|---|
| **현행** | **67.6%** | **50.0%** | **24.6%** | 100.0% |
| `REACT:3` (서브썬더 14→6) | 63.6% | 41.8% | **0.0%** | 99.5% ⚠ |
| `EXACT_REACT` (서브썬더 14→4) | 60.4% | 31.9% | 7.0% | 100.0% |

**"썬더 실점"으로 분류되던 패배가 다른 이름의 패배로 옮겨갔을 뿐이다.**
게다가 `REACT:3` 은 썬더 서브까지 한 점 깨뜨렸다(99.5%).
현행 설정이 **썬더 봇 상대로도** 가장 강하다.

### 6.6 남은 유일한 탈출구

단일 위치로는 못 막으므로, 남은 길은 **상대에게 네트 앞 공격권 자체를 주지 않는 것**뿐이다.
4번(배치 + 외삽)이 그 시도였고 실패했다. 실패 원인은 명확하다 — 상대의 도달 가능성을
아무리 잘 외삽해도, **상대는 우리가 고른 리턴을 보고 나서 움직인다**. 우리의 리턴 선택이
상대의 위치를 바꾸는 되먹임을 한 수 앞만 보는 모델로는 담을 수 없다.

제대로 하려면 2수 이상의 게임 트리(우리 리턴 → 상대 최선 응수 → 우리 수비 가능성)를
풀어야 하고, 이는 현재 판단 예산(틱당 44µs)에서 가능한 규모다. 다음 단계로 권한다.

관련 코드는 전부 남겨두었고 기본값만 0/OFF 다:
`PUSH_DEEP` `GIVE_TH` `THREAT` `OPP_EXTRAP` `TIER1` `EXACT_REACT` `GD_SERVE_GUARD` `REACT`.

---

## 7. 2수 게임 트리 (리턴 배치)

§6.6 에서 예고한 대로 1수 배치는 전부 실패했다. 원인은 **상대가 우리 리턴을 보고 나서
움직이기 때문**이고, 그 되먹임은 2수로만 풀린다.

```
우리 리턴  →  상대가 낙하지점으로 이동해 가장 위험한 지점에서 최선 응수
          →  그때 우리가 방어 가능한 코스의 가중 여유(margin)
```

이 값을 최대화하는 리턴을 고른다(maximin). 구현은 `twoPlyCoverage`.

구현 중 걸린 함정 두 가지 — 둘 다 "값이 안 변하는" 증상으로 나타났다.

1. **이진 커버리지는 포화한다.** "막는다/못막는다"로 세면 비행시간이 긴 응수는 전부
   막을 수 있어 값이 1.000 으로 붙는다. 가중 여유(−60~40)를 연속값으로 써야 변별된다.
2. **후보가 전부 이웃이었다.** 목표 x 를 2px 씩 훑으면 후보 77개가 나오지만 도달 위치가
   18px 격자라 [P5] 실제로는 2~6개의 (접촉 프레임, 접촉 위치) 로 수렴한다. 점수 상위 K개를
   뽑으면 전부 같은 선택지라 2수 값이 동일해 순위가 **절대** 안 바뀐다.
   → 접촉 위치별로 중복 제거한 뒤 비교해야 한다.

결과: 종합 67.6 → 69.5%, 리시브 50.0 → 54.1%, **vs v5_1 리시브 24.6 → 35.1%**.
`W.TWOPLY` 는 400 이상에서 포화하므로 사실상 동점 파훼자로 작동한다(기본 1000).

---

## 8. 공격 옵션 복원

수비형으로 재설계하면서 공격이 과도하게 억제됐다. 게이트별 차단 횟수를 실측했다
(`v6dev/diag_attack.mjs`, 291랠리).

```
공이 파워히트(강타)   8487  ← 압도적 1위
접촉까지 여유<14f     4602
터치 3회 이상         1176
게이트 통과           4867  → 그런데 실제 공격은 62회 (1.3%)
```

두 군데가 문제였다.

**(1) `!ball.isPowerHit` 이 중복 조건이었다.** v5 가 무너진 원인은 "빠른 강타가 날아오는데
점프"였고, 그건 `GD_ATTACK_VMAX(|xV|≤12)` 가 이미 막는다(실측 차단 0건 → 상대 파워히트의
|xV| 는 10 또는 20 이고 20은 VMAX 에서 걸린다). `isPowerHit` 는 기회의 절반 가까이를
공짜로 버리고 있었다. → 종합 69.5 → **72.6%**, 리시브 54.1 → **60.5%**.

**(2) 게이트를 통과해도 탐색이 98%를 버렸다.** `findKillJump` 의
`if (drop > 14 && !unreachable && !throughBall) continue;` — "스매시 후 14프레임 안에
떨어지지 않으면 버린다"가 사실상 모든 킬을 잘라내고 있었다. 26 으로 완화하면 포화한다.
`MAX_CONTACT` 13 → 18 도 함께. → 종합 72.6 → **75.8%**, 리시브 60.5 → **66.7%**.

교훈: 수비 강화는 공격 경로의 **가드 조건**이 아니라 **탐색 제약**에서 먼저 새어나간다.
게이트 차단 사유를 항목별로 세는 계측이 없었으면 못 찾았을 것이다.

---

## 9. 최적화

동작을 바꾸지 않고 판단 비용만 줄였다(전후 결과 완전 동일: 75.8% / 66.7%).

| 항목 | 전 | 후 |
|---|---|---|
| 수신 판단 | 56~74µs | **12~13µs** |
| p50 / p99 / 최악 | 56 / 381 / 10,717µs | **13.6 / 129 / 318µs** |

1. **후보 중복 제거.** 목표 x 77개가 18px 격자 때문에 2~6개의 (접촉 프레임, 접촉 위치)로
   수렴한다. 같은 쌍은 점수가 완전히 동일하므로 한 번만 채점한다. → 4~5배
2. **꺼진 항의 비싼 계산 제거.** 배치항이 기본 0 인데도 `worstSpikeThreat`(후보당 4 롤아웃)과
   상대 접촉 탐색을 매번 돌고 있었다. 가드 추가.
3. **스트리밍 롤아웃.** 대기 계획은 각 위협의 "우리 코트 지상 접촉점"만 필요한데 궤적 배열을
   통째로 만들고 있었다(위협당 ~100객체 × 최대 80위협). `rolloutPts` 로 접촉점만 수집.
   → 최악 지연 529 → 318µs

물리 모델은 최적화 후에도 실엔진과 비트 단위 일치를 유지한다(`verify_gd.mjs` 재검증 통과).

---

## 10. v5 는 왜 안 되는가 — 최적반응 상대와 스타켈베르크 게임

§6 에서 "코트 기하 때문에 어떤 단일 위치도 두 코스를 다 막을 수 없다"고 정리했다.
이번에 그보다 강한 사실을 실측했다. **상대가 우리 위치를 보고 코스를 고른다.**

`v6dev/diag_response.mjs` — 상대의 네트 앞 파워히트를, 그때 우리가 네트에서
얼마나 떨어져 있었는지로 나눠 집계했다.

| 우리 위치(네트까지) | v5 내리꽂기 비율 | v5_1 | v4 |
|---|---|---|---|
| 24~47px (앞) | **0.0%** (수평 강타 56/56) | 91.2% | 100% |
| 48~71px | - | 74.4% | 100% |
| 72~95px | - | 97.3% | 100% |
| 96~119px (뒤) | **100.0%** (4/4) | 16.7% | - |

**v5 는 완벽한 최적반응자다.** 우리가 앞에 서면 100% 깊은 수평 강타,
뒤에 서면 100% 네트 앞 내리꽂기. v5_1·v4 는 위치와 무관하게 내리꽂기를 고수한다
(그래서 각각 32.6% / 78.9% 를 받아낸다).

이게 v5 리시브가 0% 인 진짜 이유다. 우리가 선수(先手)로 위치를 정하고 상대가 후수로
최적반응하는 스타켈베르크 게임인데, 이 접촉점에서는 **모든 우리 위치에 대해 상대의
최적반응이 존재**한다 → 게임의 값이 0.

### 물리적 확인

`v6dev/dbg_flat.mjs` — v5 의 수평 강타에 대해 `planReceive` 가 뭘 찾는지:

```
우리 x=140 -> 접촉 가능      우리 x=152 -> 슬라이딩으로 접촉 가능
우리 x=164 -> 계획 없음      우리 x=176 -> 계획 없음      우리 x=182 -> 계획 없음
```

x>=164 에서는 "막기 어려운" 게 아니라 **어떤 접촉도 물리적으로 불가능**하다.
그런데 우리 중립 위치는 182 이고, 우리가 182 에 서 있으니 v5 는 그 코스만 고른다.

(§6.4 에서 "수평 강타는 슬라이딩으로도 못 받는다"고 쓴 것은 부정확했다.
`truth.mjs` 가 다이빙 시작 시점을 상대 타격 5프레임 뒤부터만 훑었기 때문이다.
`truth_dive.mjs` 로 전수 검증하니 **상대 타격 +1~+4 프레임 안에 시작하면 x<=154 에서
받을 수 있다.** 다만 그 자리는 v5 가 내리꽂기로 갈아타는 자리다.)

### 시도와 결과

| 개입 | 종합 | 리시브 | vs v5 리시브 |
|---|---|---|---|
| **현행** | **75.8%** | **66.7%** | 0.0% |
| 조건부 코스 예측으로 위협 가중 재조정(`COND`) | 65.6% | 45.6% | 0.0% |
| 착취당하는 위치 금지(`COND_BAN`) | 64.1% | 41.5% | 0.0% |
| 위 금지, 임계 0.8 | 67.7% | 50.6% | 0.0% |
| 중립 위치 뒤로 이동(`NEUTRAL_SHIFT` 12~40) | 75.8% | 66.7% | 0.0% |
| 위상 기반 정확 반응 지연(실제 타격 프레임 기준) | 59.8% | 30.4% | 0.0% |

**어느 것도 v5 에게서 단 한 랠리도 못 가져왔다.** 뒤로 물러나면 v5 가 내리꽂기로
갈아타 그쪽을 잃는다 - 최적반응 상대에게 결정론적 재배치는 정의상 통하지 않는다.

부수적으로 확인된 것: `REACT=4`(최악값)는 틀린 값이지만(실제는 위상에 따라 1~3)
그대로 두는 편이 낫다. 정확한 값을 쓰면 도달 범위가 낙관적이 되어 "어디든 다 막는다"로
포화하고, 그러면 최적화가 여유 없는 자리를 고른다. 4 는 사실상 **요구 안전여유**로
기능하고 있다.

### 남은 길

결정론적 위치로는 끝났고, 두 방향만 남는다.

1. **혼합전략.** 상대가 관측하는 시점(타격 결정 틱) 이후에 우리 위치를 무작위화한다.
   스타켈베르크 값이 0 이어도 내시 혼합전략의 값은 0보다 크다. 다만 18px 격자에서
   1~4프레임 만에 의미 있는 이동은 불가능하므로, 실제로는 "선제 슬라이딩을 코스
   예측에 걸고 던지는" 형태가 된다. 틀리면 18프레임 무제어라 기대값 계산이 필수다.
2. **접촉점 자체를 바꾸기.** v5 가 (224,171) 에서 치게 두는 한 값은 0 이다.
   2수 트리(§7)는 우리 리턴 1수만 본다. 3수(우리 리턴 -> 상대 응수 -> 우리 리턴 ->
   상대의 다음 공격 지점)까지 봐야 "상대를 네트에서 떼어놓는" 리턴이 평가된다.
   비용은 현재 p50 13.6µs 이므로 여유가 있다.

---

## 11. 수직 스파이크 검증 · 리턴 각도

세 가지를 물리로 확인했다.

### 11.1 온전한 수직 스파이크 (`v6dev/truth_vertical.mjs`)

엔진상 파워히트 직후 `yV = max(15,|yV_in|) * yd * 2` 이고 `|yV| <= 40` 으로 클램프된다.
따라서 상대가 받는 공의 `|yV| >= 20` 이면 **항상 최대치 40** 이 나온다 = 온전한 수직 스파이크.
`xV` 는 `(|xd|+1)*10` 이므로 최소 10 (가장 수직).

| 상대 타격 x | 우리 코트 접촉창 | 즉시 방어 가능 위치 | 관측 후 반응(≤4f) |
|---|---|---|---|
| 200 | k2 @ 180 (1프레임) | p = 132~184 | **불가** |
| 212 | k2 @ 192 (1프레임) | p = 144~184 | **불가** |
| 224 | k2 @ 204 (1프레임) | p = 156~184 | **불가** |
| 236 | **없음** (네트 기둥에 되튕겨 자책) | - | - |
| 248 | **없음** (자책) | - | - |

두 가지가 확정됐다.

* **반응은 물리적으로 불가능하다.** 접촉창이 1프레임이라 관측 후 어떤 이동도 늦는다.
  오직 미리 그 자리에 서 있어야만 막힌다.
* **위험 구간은 좁다.** 상대 타격 x 가 236 이상이면 그 스파이크는 네트 기둥 밴드
  (`|x-216|<25 && y>192`)에서 **자기 코트 쪽으로 되튕겨 자책**이 된다.
  즉 실제 위험은 네트 바로 건너편 x ≈ 190~232 뿐이다.

세 위험 지점을 모두 덮는 위치는 p = 156~184 이고, 현재 중립 위치 182 는 그 안에 있다.
**온전한 수직 스파이크는 이미 커버되고 있다.**

### 11.2 우리 리턴이 내주는 각도 (`v6dev/diag_danger.mjs`)

"우리가 넘긴 공이 상대에게 막을 수 없는 스파이크 각도를 주는가"를 위험도 0~3 으로 쟀다
(2 = 상대가 우리에게 1프레임 접촉창을 강요할 수 있음, 3 = 접촉창 0).

| 상대 | 지상 리턴 (위험 0/1/2) | 점프 공격 (위험 0/1/2) |
|---|---|---|
| v5 | 0% / 0% / **100%** | **100%** / 0% / 0% |
| v5_1 | 22% / 1% / **77%** | 48% / 12% / 40% |
| v4 | 37% / 0% / **63%** | 50% / 50% / 0% |

**직관과 반대 결과다.** 각도를 내주는 쪽은 점프 공격이 아니라 **지상 리턴**이다.
파워 스매시는 아래로 꽂혀서 상대가 퍼올려야 하므로 오히려 가장 안전한 리턴이다.
(v5_1 상대 점프 공격의 40% 는 예외이고, 여기서 "점프 후 각도를 내줬다"는 관찰이 나온 듯하다.)

### 11.3 "다른 각도로 보내기" — 구현했고, 역효과였다

`returnDanger(tr)` 를 구현했다. 상대 위치를 전혀 쓰지 않는 **순수 기하 지표**라
(궤적이 위험 구간을 상대가 때릴 수 있는 높이로 지나는가) 최적반응 상대에게도 견고하다 —
§6·§10 의 배치 시도들이 실패한 이유(상대 위치 추정이 어긋남)를 원리적으로 피한다.

안전한 각도는 **실제로 존재한다** (`v6dev/dbg_angles.mjs`):

```
공 (150,232) yV=34 를 받을 때
  접촉 px=120 → 반사 xV=+10 → 착지 430 → 위험도 0
  접촉 px=136 → 반사 xV= +4 → 착지 262 → 위험도 2
```

그런데 목적함수에 넣으면 성적이 **단조 감소**한다.

| `RET_DANGER` | 종합 | 리시브 |
|---|---|---|
| **0 (채택)** | **75.7%** | **66.1%** |
| 800 | 68.4% | 51.7% |
| 3000 | 63.1% | 38.9% |

행동은 분명히 바뀐다(가중 3000 에서 v4 지상 리턴 위험0 비율 37% → 67%,
v5_1 점프 공격 48% → 96%). 그런데 결과는 나빠진다. 이유는 지표가 **너무 보수적**이기
때문이다 — "상대가 어디든 갈 수 있다"고 가정하므로, 이론상 때릴 수 있을 뿐 실제로는
닿지 못하는 깊고 강한 리턴까지 버리고 대신 얕고 안전한 공을 고른다.

그리고 **v5 상대로는 가중치 3000 에서도 위험도가 100% 그대로 2 였다.**
즉 v5 가 보내는 깊고 빠른 수평 강타를 받는 지점에서는, 네트를 넘기는 모든 리턴이
상대에게 1프레임 킬을 내준다. **"다른 각도"가 물리적으로 존재하지 않는다.**

코드는 남기고 기본 `RET_DANGER = 0`. 지표 자체는 정확하므로,
"상대가 실제로 그 지점에 도달할 수 있는가"를 결합한 버전이 나오면 재검토할 가치가 있다.

---

## 12. 기하 × 도달성 결합 — 그리고 드러난 설계 원리

§11.3 의 `returnDanger`(도달성 무시, 과보수)와 §6·§7 의 배치·2수(행동 예측, 과낙관)를
결합했다. 옳은 결합은 "예측"이 아니라 **도달 가능 집합**이라고 보고 구현했다.

```
프레임 k 에 상대가 x 를 때릴 수 있다
  <=>  |x - opp.x| <= 32 + 6*(k - 반응지연)      (가로: 걸어서 닿는가)
  AND  y >= oppLowY[k] - 32                      (세로: 점프로 그 높이까지 오르는가)
```

`returnDangerReach` (§ `gd_module.js`). **지표로서는 정확히 작동한다** — 같은 리턴을
두 지표로 재보면 도달성 게이트가 유의미하게 걸러낸다.

| 상대 | 기하 2 → 결합 0 (실제로는 못 때림) | 기하 2 → 결합 2 (진짜 위협) |
|---|---|---|
| v5_1 | **26%** | 43% |
| v4 | **16%** | 43% |
| v5 | 0% | **50%** |

v5 만 100% 진짜 위협으로 남는 것도 §10 과 정확히 일치한다.

### 그런데 성적은 개선되지 않는다

| 적용 방식 | 종합 | 리시브 |
|---|---|---|
| **미적용 (채택)** | **75.8%** | **66.7%** |
| 결합 위험도 감점 60 / 140 / 250 | 75.8% (완전 동일) | 66.7% |
| 결합 위험도 감점 400 | 75.1% | 65.0% |
| 결합 위험도 감점 1000 | 67.5% | 49.4% |
| 2수의 상대 모델을 도달 가능 집합으로 교체 | 69.0% | 53.5% |

가중치 250 이하에서는 **판단이 한 번도 안 바뀐다**(후보 간 점수 차가 그보다 크다).
그 이상으로 올리면 깊이·네트 통과 같은 본질 항을 눌러버려 나빠진다. 쓸 창이 없다.

### 드러난 설계 원리 — 비대칭 보수성

이번 세션의 모든 실패를 한 줄로 정리하면 이렇다.

> **상대의 능력을 넓게 잡는 모델은 예외 없이 성적을 떨어뜨렸다.**

| 개입 | 상대를 어떻게 가정 | 결과 |
|---|---|---|
| `returnDanger` | 어디든 도달 | −12%p |
| `RET_DANGER2` 고가중 | 도달 가능 집합 | −8%p |
| `SPIKE_REACH` | 도달 가능 집합 | −7%p |
| `COND` / `COND_BAN` | 항상 최적반응 | −10%p |
| 정확 반응지연(우리에게 관대) | (상대적으로 상대를 넓게) | −16%p |

반대로 **채택된 설정의 구조는 일관되게 비대칭**이다.

* **우리 자신은 보수적으로**: `REACT = 4`(최악값). 실제는 위상에 따라 1~3 이지만,
  정확한 값을 쓰면 "어디든 다 막는다"로 포화해 최적화가 여유 없는 자리를 고른다.
  4 는 사실상 **요구 안전여유**로 기능한다.
* **상대는 현실적으로**: 2수의 상대 접촉은 "낙하지점으로 걸어간다"는 행동 예측(±8px).

이유는 명확하다. 양쪽 모두에 편집증적이면 **모든 선택지가 똑같이 나빠 보여
최적화의 판단이 잡음이 된다.** 변별력이 사라지는 것이 안전을 잃는 것보다 비싸다.
수비 목적함수를 설계할 때 "최악을 가정하라"는 직관은 자기 자신에게만 적용해야 한다.

코드는 전부 보존하고 기본값만 0/OFF: `RET_DANGER` `RET_DANGER2` `SPIKE_REACH`.
지표는 정확하므로 진단 도구로는 계속 쓸 값어치가 있다(`v6dev/diag_danger2.mjs`).
