# v11 최적화 기록

원본은 보존하고 `Lion_Eating_Bank_v11_optimized.js`에 별도로 적용했다. 이번 파일은 보존된 `tools/v11/original.js`와 `tools/v11/build.mjs`로 다시 생성한 동일 코드다.

## 변경

- 점수 변경 시 접촉 횟수 초기화: 새 랠리 첫 관측이 41~43이어도 이전 접촉 횟수가 남지 않도록 수정.
- NEW_GAME_RESET=1: 새 게임에서 이전 서브 학습 초기화. 같은 상대와 연속 세트에서도 다시 학습하는 비용이 있다.
- 상대 선이동 학습 초기화 보완.
- kgOppMotion의 공중 위치 반복 계산을 동일한 등차수열 계산으로 대체.
- DEBUG=false, 로그 상태 명시, 과거 실험 헤더 주석 정리. 공격·수비 전술 변경 없음.

## 이전 검증 결과

공중 위치 계산 488,755건 동일. 상대 v10 및 mixed_v2_adaptive, 좌우·지연 1/2프레임의 8경기에서 원본과 수정본 13,724결정의 출력 불일치 0건. 내부 코어·오케스트레이터 예외 및 잘못된 출력 0건. 접촉 초기화와 밴딧 초기화는 별도 회귀 사례로 확인했다.

Node 평균 시간은 원본 0.226ms, 수정본 0.228ms로 평균 속도 향상은 확인되지 않았다. Chrome Worker 실경기, 당일 스킬, 360ms timeout·15회 Worker 재시작은 검증하지 않았다. 관측 사이 접촉 누락과 같은 랠리의 5번째 접촉 실점까지 해결한 것은 아니다.

재현: 프로젝트 루트에서 `node tools/v11/test.mjs`. 생성 스크립트는 파일을 직접 저장하지 않고 검토·적용할 패치를 출력한다.

첨부 원본 SHA-256: `1018FECDFB1D5087514DADC03B448FE3329C898E516DEEF57A62237559FE8601`.
정규화 사본 SHA-256: `de64300d4c5c1b7975b00c2b23795f239d0c3fc6165b42acba14c77544089c10`.
