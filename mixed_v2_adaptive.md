# mixed_v2_adaptive — 반복해서 막힌 공격의 즉시 교체

## 최신 추가: 수평 타격 → 뒤쪽 벽 → 즉시 낙하

최신 실행 파일은 같은 이름의 mixed_v2_adaptive.js다. 스파이크 내성을 감지한 뒤 처음 선택하는 패턴을 **wallgraze**로 바꿨다. 아래의 나머지 문서는 이 추가 전의 반복 제어 작업 기록이다.

- 왼쪽 공격의 실제 성공 타점: 공 (190, 171), 타격 직후 속도 (20, 0).
- 오른쪽의 대칭 타점: 공 (242, 171), 속도 (-20, 0). 좌우 정규화로 같은 탐색을 사용한다.
- 좌표 원점은 왼쪽 위다. 네트 x=216, 뒤쪽 벽 x=432 또는 0, 공의 지면 접촉 y=252다.
- 해당 공은 왼쪽에서 12프레임째 (430, 237), 13프레임째 벽 반사 후 (410, 249), 14프레임째 지면에 닿는다. 엔진이 이산 프레임 단위로 반사하므로 시각적으로 벽 표면을 정확히 접선처럼 스치는 기하학적 보장은 아니다.
- 하향 스파이크가 두 번 연속 받아쳐지거나 최근 확인된 결과에서 실패 비율이 높으면 이 공격 상태로 전이하고 기존 공격 경로를 취소한다.
- 탐색에서 수평 파워 타격, 천장·네트 선행 충돌 없음, 상대쪽 바닥 착지, 뒤쪽 벽 반사 후 0~2프레임 이내 지면 접촉을 모두 만족해야 이 패턴으로 인정한다.
- 타점을 (190,171)로 고정하는 것이 아니라 현재 공과 선수 상태로 셋업·접촉 경로를 계산한다. 불가능한 상황에서 공의 위치나 속도를 조작하지 않는다.

엔진의 발사 후 궤적 5,096개를 검사했고, 이 조건에 맞는 좌표 조합은 1,372개였다. 이는 상대 접촉을 제외한 탄도 검사이며 모든 좌표가 실제 랠리에서 도달 가능한 타점이라는 뜻은 아니다. 실제 타격과 득점은 별도 대전 시험으로 확인했다.

최신본 v3 상대 40경기(좌우 교대, seed 718341, 1프레임 입력 지연): 20승 20패, 득실점 279:281. **벽 반사 후 즉시 낙하 직접 득점 13회** 확인. 접촉 예시는 (190,171), (20,0), 반사 13프레임·지면 14프레임이다. 2프레임 입력 지연 20경기(seed 923417)는 10승 10패이며 내성 전환이 관측되지 않아 이 패턴의 직접 득점도 0회였다.

최종 60경기 집계 오류 0회, 명시적으로 허용한 공격 방향 변경 외 보호 출력 검사 위반 0회. 원본 GD·ACCore 동일성, 내성 상태 전이, 잘못된 높은 벽 반사/네트 우회 후보 배제, 기존 선수 이동 보존 검사 통과. 실제 브라우저 시험 및 모든 상대에 대한 승률 개선은 보장하지 않는다.

```powershell
node tools/search-wallgraze.mjs
node tools/test-resistance-guards.mjs
node tools/test-resistant.mjs 40 1 718341 wallgraze
node tools/test-resistant.mjs 20 2 923417 wallgraze
```

---

## 이전 작업 기록

이번 수정은 기존 대체 공격의 발동과 반복 제어를 바꾼다. mixed_v2.js 원본은 수정하지 않았다. 브라우저 봇 설정에서 mixed_v2_adaptive.js를 다시 불러와야 한다.

## 바뀐 규칙

- 네트 하향 스파이크를 상대가 **두 번 연속 받아내면** 다음 공격을 교체한다. 최근 결과 3~4개 중 두 번 이상, 60% 이상 막혀도 전환한다.
- 전환할 때 이미 저장한 기존 공격 경로를 버리고 다시 탐색한다.
- 내성 대응 중에는 같은 하향 스파이크를 단순한 차선 후보로 남겨두지 않는다.
- 천장 경유 → 뒤쪽 벽 → 수평 코스 → 다른 낙하 각도를 후보 유형으로 사용한다. 대체 공격도 두 번 연속 실패하면 우선 유형을 바꾼다.
- 각도 변경형 하향 공격은 마지막으로 막힌 공격의 예상 낙하지점과 선수 폭 두 배 이상 다른 위치로 떨어지는 경로만 허용한다. 실제 현재 궤적으로 계산한다.
- 대체 공격 결과를 확인하면 반복 횟수를 초기화하고 다음 직접 공격을 다시 평가한다. 대체 공격만 영구 강제해 약한 코스를 계속 내주는 것을 피한다.
- 유효한 대체 경로를 못 찾았을 때도 우리 공격권의 공중 타격을 같은 하향 입력으로 조용히 되돌리지 않고 수평 입력으로 바꾼다.

## 감지 오류 수정

수평 파워 타격도 중력 때문에 몇 프레임 뒤 수직 속도가 양수가 된다. 이전 코드는 이를 하향 스파이크로 잘못 분류할 수 있었다. 이번에는 관측 간격 동안 더해질 수 있는 중력 속도와 구분한다.

상대 접촉은 관측 궤적과 접촉 없는 예측 궤적을 비교해 추정한다. 상대 소스나 숨은 상태를 읽지 않는다. 감지 누락과 입력 지연 때문에 실제 반복을 완벽히 없앤다고 보장하지는 않는다.

## 수비와 공격 방향의 구분

기존 GD와 ACCore 원문은 보존한다. 지상 리시브·슬라이딩·회복 입력은 유지한다.

기존 코어가 공중 파워 타격 중 아래로 보내려는 경우에도, 내성 대응 상태이고 공이 우리 진영이면 방향만 수평으로 바꾼다. x와 hit는 그대로 두고 y=1을 y=0으로 바꾼다. 두 입력 모두 점프를 요청하지 않는다. 따라서 즉시 선수 이동·착지·슬라이딩 동작은 동일하며, 공에 닿은 뒤 발사 방향만 달라진다.

이 방향 전환은 명시적인 공격 출력 변경이다. “모든 수비 경로의 최종 입력이 바뀌지 않는다”는 뜻은 아니다. 이후 공 궤적과 경기 결과는 달라질 수 있다.

## 이번 최종본 검증

| 상대 / 지연 / seed | 경기 수 | 승 / 패 / 무 |
|---|---|---|
| v3 / 1프레임 / 718341 | 20 | 10 / 10 / 0 |
| Defense / 1프레임 / 317953 | 20 | 8 / 10 / 2 |

10점 또는 시뮬레이션 240초 제한. 시간 종료 시 점수 우세도 승으로 집계한다. 이것은 전체 승률 향상을 증명하는 결과가 아니라, 공격 반복 제어를 변경한 버전의 회귀 시험이다.

- 최종 40경기 집계된 실행 오류 0회.
- 공격 방향 변경으로 명시한 구간을 제외한 보호 출력 검사 위반 0회.
- Defense 상대 20경기에서 공격 방향 전환 82회 관측.
- 두 번 리시브 후 전환, 수평 타격 분류, 대체 공격 실패 후 유형 변경, 기존 경로 후보 제외, 빈 탐색 시 하향 반복 방지 검사 통과.
- 실제 엔진의 선수 상태 200개에서 기존 하향 입력과 수평 전환 입력을 각각 3프레임 비교: 이동·착지·슬라이딩 상태 600쌍 일치.
- 좌우·선수 상태별 수비 보호, 원본 코드 동일성, 문법 검사 통과.
- 실제 브라우저 검증은 별도다. Node 가상 Worker 지연으로 시험했다.

공중 반사는 엔진의 속도 규칙을 바꾸지 않는다. 벽은 수평 방향을 반전시키고 천장은 수직 속도를 낮춘다. 상대가 절대 대응하지 못하는 속도나 무조건 득점은 보장하지 않는다.

## 재실행

프로젝트 루트에서:

```powershell
node tools/test-resistance-guards.mjs
node tools/test-resistant.mjs 20 1 718341 ceiling
node tools/test-resistant.mjs 20 1 317953 ceiling Lion_Eating_Bank_v6_Defense.js
```

구현: tools/mixed-resistance.js, 조립: tools/build-resistant.mjs. 단일 실행 파일: src/code-here/mixed_v2_adaptive.js.
