# Lion_Eating_Bank_v10 — 기능 설명

2026-09-04 밤. **v8(= v9 본문) 위에 썬더 없는 랠리의 약점을 고친 것.** v9 는 v8 의 출력 동일 정리(STEP 6)이고, v10 은 같은 생성기에서 STEP 6 을 포함해 STEP 17 까지 적용한 결과다.
기본 빌드는 **썬더 서브 OFF**(`THUNDER_SERVE=0`): 서브도 평소 랠리 로직이 맡는다.
생성기·검증 도구는 개발 저장소 jimin_pika 의 `bot-dev/` 에 있다(이 저장소에는 봇 파일과 이 문서만 올린다). **파일을 손으로 고치지 말고 생성기를 고칠 것**(당일 노브만 예외).

```
V8_THUNDER=0 node --no-warnings bot-dev/v8/build_v8.mjs 17 src/code-here/Lion_Eating_Bank_v10.js   # v10 (본문 동일 재현 확인)
V8_THUNDER=0 node --no-warnings bot-dev/v8/build_v8.mjs 6  out.js                                      # = v9 본문
node --no-warnings bot-dev/v4_checks.mjs src/code-here/Lion_Eating_Bank_v10.js                         # 12/17 (썬더 OFF 빌드는 5개 FAIL 이 정상)
```

설계 원칙(사용자): ① 수비는 최소 이동으로 상대의 모든 경우를 막는 자리 ② 점프는 확정킬·안전한 넘기기에만 ③ 다이빙은 걸어서 못 닿고 다이빙으로 닿을 때만.
판정은 엔진 물리·정확 모델 대조를 우선하고, 벤치(우리 봇 풀 + mixed_v2 결정론)는 회귀 검증용으로 썼다.

---

## 1. v8 → v10 에서 바뀐 것 (생성기 STEP 7~17)

배경: 썬더를 끄면 v8 은 mixed_v2(네트 앞 32~58px 에 서서 점프 정점에서 내리꽂는 상대)에 3-45(랠리 29%, 서브 14%)로 진다. 실점의 71% 가 "내 로브 → 상대가 정점(y≈108)·네트 27px 에서 내리꽂기 → 착지 13~42px, 나는 56px" 였고, RIGHT 에서는 안전 넘기기끼리의 무한 랠리가 8/24 경기. 분석 문서 `bot-dev/v8/VS_MIXED_V2_FINDINGS.md`.

| STEP | 이름 | 내용 | 근거 |
|---|---|---|---|
| 7 | DEF-A | 수비 여유 모델의 **내 반응을 3→4**(관측 3 + 지연 1 의 최악 위상), 후보 x 에 **도착 제약**(접촉 k 스텝 전까지 걸어서 닿는 자리만), 표준 위협에 점프 정점(y108) 접촉 추가 | 반응 3 이면 정점 내리꽂기를 여유 +3 으로 "도달 가능"으로 봐 1프레임 차로 놓침. 이득의 대부분은 반응 4 |
| 8 | PASS-SIM | **넘기기 시뮬**: 지상 넘기기 후보마다 넘긴 공에 대한 상대 접촉(걷기·점프·다이빙, 지평 90스텝) × 파워히트 6종 뒤 내 최악 여유를 채점. 여유 ≥ 0 이면 항상 우선, 아니면 부족분 ×10 감점 | 예전 채점은 "상대 x 에서 먼 착지 − 체공×2" 라 네트 위를 높게 지나는 로브가 살아남았다 |
| 8 (1f 세션) | GATE-PRE | 킬 게이트가 공중(state 1/2) 상대의 y·vy 를 내 접촉까지의 pre 프레임만큼 먼저 진행(착지하면 지상) | gap_audit: 공중 상대 게이트 6.6% 가짜 킬 |
| 10 | KG-PRE | 위 GATE-PRE 와 같은 수정의 노브(`PRE_AIR`), 접촉 전 자유 걷기 비율 `PRE_WALK`(기본 0) | |
| 11·12 (1f 세션) | DIVE-CONTACT · OPP-DIVE | 수비 접촉 후보에 상대 다이빙 도달 추가, 다이빙 중(state 3) 상대를 수동적 몸으로 모델 | gap_audit. 결과 변화 0, 정확 모델과 941/941 일치 |
| 13 | FA-ARM | fastAttack 의 팔 준비 프레임 `ARM_UNTILS` [2,3] → [4] | 점프 명령은 한 틱(3)+지연 1 동안 유지되므로 스매시 x 이동은 5프레임째부터. 예전 값은 6~12px 낙관해 **허공 점프**(12시드 26점) |
| 14 | STALE | **교착 감지**: 내 마지막 터치가 주기 1~3 터치로 반복(접촉 자리 ±6px, 반사 xV ±2)되면 같은 접촉 자리 후보 감점 | 안전 넘기기끼리의 fixed point(구석 x400 ↔ 상대 로브, 72프레임 주기) |
| 15 | SET-SAFE | 상대가 닿을 수 있는 세트(hover) 후보 제외 | 네트 앞 12px 세트는 네트 너머 32px 상대가 뛰어서 닿음 |
| 16 | STALE-JUMP | 교착인데 다른 접촉 자리 후보가 없으면(구석) 계획을 비워 passJump 경로로 | 구석은 co-sim 후보가 하나뿐이라 감점만으로 못 바꿈. 무한 랠리 6→0 |
| 17 | SERVE-PASS | **서브 첫 터치는 세트하지 않고 넘긴다** | 서브 세트(네트 앞 12px) 뒤 두 번째 터치가 전부 수직 로브뿐 → 상대 fastAttack 3터치 실점(12시드 26회) |

STEP 9 DIG(교차 후보 없으면 디그), 12 KG-WALK(접촉 전 자유 걷기), 13 SELF-VY(내 공중 vy 점프표 역산), 16 SET-ORDER(안전 넘기기 > 세트)는 만들었으나 기각(§5). 노브로만 남아 있다.

---

## 2. 당일 노브 (v8 표 + 추가분)

| 노브 | 기본 | 뜻 |
|---|---|---|
| `THUNDER_SERVE` | **0** | 썬더 서브 끔. 1 로 하면 v8 의 3위상 시퀀스·밴딧·이탈이 살아난다(그 경우 서브 랠리 100%, mixed 46-2 는 이 값이었다) |
| `DEBUG` | true | F12 로그(1랠리 1줄). 제출 전 false 권장이나 true 여도 무방 |
| `DEF_CFG.REACT` | **4** | 내 반응 지연(물리 스텝). v8 은 3 |
| `DEF_CFG.DEF_ARRIVE` | 1 | 수비 후보 x 도착 제약 |
| `DEF_CFG.CANON_SET` / `DEF_RULE` | wide / minmax | 표준 위협 집합 / 최악 여유 최대화(`cover` = 막는 샷 수 최대화는 기각) |
| `DEF_CFG.PASS_SIM` / `PASS_SIM_W` / `PASS_SIM_HORIZON` | 1 / 10 / 90 | 넘기기 시뮬 채점 |
| `DEF_CFG.PASS_DIG` | 0 | 디그(기각) |
| `KILL_GATE.PRE_AIR` / `PRE_WALK` / `OPP_DIVE` | 1 / 0 / 1 | 공중 상대 pre 진행 / 접촉 전 자유 걷기(기각) / 다이빙 상대 모델 |
| `DEF_CFG.DIVE_CONTACT` | 1 | 수비 접촉 후보에 상대 다이빙 |
| `FAST_ATTACK_CFG.ARM_UNTILS` | [4] | v8 [2,3] |
| `DEF_CFG.STALE_N` / `STALE_JUMP` | 3 / 1 | 교착 감지 주기 수 / 교착 시 passJump 경로 |
| `DEF_CFG.SET_SAFE` / `SET_ORDER` / `SERVE_SET` | 1 / v8 / 0 | 상대가 닿는 세트 제외 / 세트 우선순위 / 서브 첫 터치 세트 금지 |
| `estimateMyVy` (SELF_VY) | v8 식 | 정확한 점프표 역산은 맞는 모델이지만 passJump 강타가 첫 공중 틱에서 취소되지 않고 실행돼 손해(§5) |
| 그 외 | v8 | STANDBY_MODE canon(대기 x 152), PRIORITY fast, TOUCH_DETECT path, KILL_GATE.PREDICATE kg/REACT 3 등 v8.md 참고 |

---

## 3. 기능 요약 (v8.md §3 + 변경분)

- **서브**(썬더 OFF): 서브 공(x 56/376 수직 낙하)의 첫 터치를 넘기기 시뮬로 골라 넘긴다(세트 금지). 상대가 서브 로브를 네트 앞에서 바로 강타하는 경우(12시드 8~10회)는 남은 약점.
- **수비 위치**: 상대 접촉 시점 × 샷 9종 궤적에 대해 내 최악 여유(반응 4·걷기 6·다이빙·상자 32)를 최대화, 후보는 접촉 전까지 걸어서 닿는 자리만. 상대 yVelocity 복원·충돌 래치·KEEP·다이빙 접촉·다이빙 중 상대 모델 포함.
- **리시브**: 빠른 공격(팔 준비 4) → 킬 점프 → 지상 몸리시브 co-sim(넘기기 시뮬 채점; 상대가 못 닿는 넘기기 > 세트(상대가 못 닿는 것만, 서브는 제외) > 가장 덜 위험한 넘기기) → 교착이면 다른 자리 또는 passJump.
- **공격**: KILL-GATE kg(REACT 3, 공중 상대 pre 진행) 그대로. 상대 선이동을 가정하는 게이트(최소최대·학습)는 정확도는 올리지만 시도가 줄어 손해라 기각(§5).
- **다이빙·터치 감지**: v8 과 동일.

---

## 4. 검증 (2026-09-04 밤)

24시드 × 좌우 = 48경기/칸, 지연 1, 10점. 경기 W-L / 랠리 승률. mixed_v2 는 빔서치가 24ms 벽시계 예산이라 비결정론이므로 **가짜 시계(예산 미소진 = 최대 강도)로 결정론화**해 쟀다(`bot-dev/v8/vs_diag.mjs … fake=1`).

| | NetCamper_v2 | AC_v5_2 | RedTeam_RL | OurBot_v12 | LionBC_v4 | mixed_v2(결정론) |
|---|---|---|---|---|---|---|
| v9 (= v8 본문, 썬더 OFF) | 48-0 / 100 | 48-0 / 99 | 48-0 / 89 | 48-0 / 99 | 18-30 / 45 | 3-45 / 29 (서브 14) |
| **v10** | 48-0 / 100 | 48-0 / 95 | 48-0 / 95 | 48-0 / 86 (서브 73) | 26-22 / 51 | **45-3 / 71** (서브 73·리시브 68), 무한 랠리 0 |

- 4만 프레임 상한 16시드: vs v9 30-2(교착 0; 주기 감지 일반화 전에는 28/32 교착), LionBC 17-15, v10 미러 16-16(교착 0).
- Chrome(headless, fast, 10점, Web Worker): AC·NetCamper·RedTeam 좌우 6전 6승(RT-v10R 10:1, 나머지 10:0), 페이지 예외 0 — `bot-dev/v8/browser_v10_results.json`.
- decide 시간 p50 0.15ms, p99 0.7ms, 최대 27ms(node).
- 상대별 킬 시도 정확도(killJump+fastAttack): AC 95%, NetCamper 65%, LionBC 55%, mixed 27%(v8 은 13%). 받힌 시도 뒤에도 랠리 승률 65% 라 시도를 줄이는 쪽이 손해.

알려진 트레이드오프
- OurBot_v12 서브 랠리 100→73%(팔 준비 4·서브 세트 금지의 대가, 경기는 48-0). `V8_FA_ARM=2,3`·`V8_SERVE_SET=1` 로 되돌릴 수 있다.
- mixed 류 상대 남은 실점: 내 로브 직접 킬, passJump 강타의 받기→세트→킬 카운터, 서브 로브 즉사. 계산량이 아니라 상대 2수 모델이 필요(§5).
- 좌우 차(mixed 상대 LEFT 79 / RIGHT 64) 원인 미조사.

---

## 5. 기각한 것과 이유 (12시드 mixed 결정론 기준, v10 = 72%)
- DIG(교차 후보 없으면 내 코트에 띄우는 디그): 17%. 디그 뒤 세트→킬 점프가 받힘.
- KG-WALK(킬 게이트에 접촉 전 자유 걷기 0.5/1): 리시브는 오르지만 서브 킬이 사라져 40~47%.
- SELF-VY(내 공중 vy 정확 역산): 41%. 모델은 맞으나 예전 오차가 passJump 강타를 첫 공중 틱에서 취소시켜 주던 효과가 사라짐.
- SET-ORDER safe(안전 넘기기 > 세트): 46%. 세트가 사라져 킬 기회 감소.
- DEF-RES(수비 격자 4·표본 16·지평 45): 60%, NC·AC 100→94. 후보가 늘면 minmax 가 더 보수적 자리를 고른다.
- KG-COVER(상대 선이동 최소최대 게이트): 정확도 13→76% 지만 시도 1/4 로 58~62%, v9 상대 9-23 스톨 14. 방향 가산·공중 재선택: 효과 0.
- PJ-SIM(passJump 후보 넘기기 시뮬 채점): 효과 0(상대 카운터는 받기→세트→킬 2수).
- OPP-PRE(상대 선이동 비율 EMA 학습 → 게이트): 정확도 17→46% 지만 시도 반감, 66%.
- ADAPT-BLEND(v4 부터 꺼진 착지 분포 학습 수비 편향 0.62/0.3): mixed 0-24, 풀 붕괴(NC 37-11, LionBC 3-45). 상대 착지가 두 봉우리라 평균으로 치우치면 둘 다 못 막는다.

---

## 6. 도구
- 생성기 `bot-dev/v8/build_v8.mjs` (STEP 1~22; env 는 각 STEP 머리말). 실험용 사본 `build_v10.mjs` 는 폐기.
- `bot-dev/v8/vs_diag.mjs` 맞대결 실점 분해(mixed 는 후킹, 그 외 상대는 내 쪽만) → `vs_diag2/3/4.mjs` 기전·공격·교차표, `attempt_value.mjs` 킬 시도 뒤 랠리 승률.
- `bot-dev/v8/bench_cap.mjs`(CAP=40000, 스톨 수), `rally_escape/cmp_bench.mjs` 풀 벤치, `bot-dev/v8/browser_cfg_v10.json` Chrome 하네스.
- 진단: `fa_trace.mjs`(허공 점프), `stale_trace.mjs`·`cand_trace.mjs`(교착·후보), `canon_probe.mjs`(표준 위협 여유), `vs_endless.mjs`(무한 랠리 재현).
